---
phase: 05-score-foundation-public-top-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stock_screener.py
  - diagnose_finnhub.py
  - tests/test_valuation_fixture.py
autonomous: false
requirements: [FIX-01, FIX-02]
must_haves:
  truths:
    - "A committed test asserts KO's Lynch/Graham buy price and discount values match hand-computed expectations within tolerance"
    - "Running the screener for KO produces Lynch_Discount_Pct and Graham_Discount_Pct that match the audited formula output (sane, not garbage)"
    - "A ticker with negative/zero EPS or negative/zero growth is no longer dropped — it is retained with a sentinel discount that the downstream scorer (Plan 02) maps to worst-possible"
    - "get_combined_data() returns an fcf_per_share field read from the already-fetched Finnhub metric=all bundle (no new HTTP request)"
  artifacts:
    - path: "tests/test_valuation_fixture.py"
      provides: "KO hand-verified spot-check fixture for lynch_metrics() and graham_metrics()"
      contains: "KO_INPUTS"
    - path: "stock_screener.py"
      provides: "FCF field passthrough in get_combined_data(); negative-input routing in process_ticker(); WORST_DISCOUNT sentinel + audit comments"
      contains: "fcf_per_share"
    - path: "diagnose_finnhub.py"
      provides: "FCF field names added to the field inspection list to confirm the exact Finnhub key"
  key_links:
    - from: "process_ticker()"
      to: "lynch_metrics() / graham_metrics()"
      via: "negative-input interception routes error-return to WORST_DISCOUNT sentinel instead of dropping the row"
      pattern: "WORST_DISCOUNT"
    - from: "get_combined_data() return dict"
      to: "Finnhub metric=all bundle (fh)"
      via: "fh.get('freeCashFlowPerShareAnnual')"
      pattern: "fcf_per_share"
---

<objective>
Diagnose and fix the deferred "Buy Price visibly wrong across all tickers" bug (FIX-01), commit a hand-verified KO spot-check fixture, and confirm the corrected discount fields are sane BEFORE any pillar scoring consumes them (FIX-02). Also lay two pieces of groundwork the Plan-02 scorer depends on: route negative/calc-breaking inputs to a worst-possible sentinel instead of dropping the row (D-01), and pass the FCF sign through from the already-fetched Finnhub bundle (D-04) for the Plan-02 trap gate.

This plan is the hard Wave-1 prerequisite for the entire phase. The composite scoring engine (Plan 02) must NOT begin until the FIX-02 gate in this plan passes — corrupt discounts would poison the Value pillar from the start.

Purpose: Guarantee Lynch_Discount_Pct / Graham_Discount_Pct read as sane before the composite consumes them (FIX-02), and supply the negative-input and FCF plumbing the scorer needs.
Output: A committed KO fixture test, audited+commented formula code, negative-input routing, and an fcf_per_share field in the combined-data dict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-score-foundation-public-top-n/05-CONTEXT.md
@.planning/phases/05-score-foundation-public-top-n/05-RESEARCH.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan introduces the following new symbols / files (later plans and the verifier depend on these exact names):

| Symbol / File | Kind | Defined in | Notes |
|---|---|---|---|
| tests/test_valuation_fixture.py | new file | this plan | Vanilla-assert KO fixture; no pytest dependency added to requirements.txt |
| KO_INPUTS | dict constant | test_valuation_fixture.py | Live-verified KO inputs (price, eps, g, dy, aaa_yield) |
| KO_EXPECTED | dict constant | test_valuation_fixture.py | Hand-computed expected Lynch/Graham outputs with tolerances |
| WORST_DISCOUNT | module constant | stock_screener.py (config block) | Sentinel discount value (e.g. -999.0) for negative-input rows; Plan 02 maps it to sub-score 0 |
| fcf_per_share | dict key | get_combined_data() return | FCF per share read from Finnhub bundle; key consumed by Plan 02 trap gate |

NOTE: NO new constants for scoring/weights/bands are created here — those belong to Plan 02. This plan only adds WORST_DISCOUNT and the FCF plumbing.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm live KO inputs and the exact Finnhub FCF field name (data verification before coding)</name>
  <what-built>
    Two [ASSUMED] facts in RESEARCH.md must be confirmed against live data before encoding the fixture and the FCF read, because both will silently corrupt downstream work if wrong:
    (1) KO's current price/EPS/growth/dividend-yield/AAA-yield (RESEARCH.md Assumption A2 — fixture values are estimates from June 2026 and will be stale at execution time).
    (2) The exact FCF field name in the Finnhub metric=all response (RESEARCH.md Assumption A1/A5 — freeCashFlowPerShareAnnual and freeCashFlowPerShareTTM are unconfirmed community-sourced names).
    Claude automates the data fetch first: add freeCashFlowPerShareAnnual, freeCashFlowPerShareTTM, epsAnnual, epsGrowth5Y, dividendPerShareAnnual to the inspected fields in diagnose_finnhub.py, run it for KO/AAPL/MSFT, and print the values. Then the user confirms the live KO numbers and the real FCF key.
  </what-built>
  <how-to-verify>
    1. Run: python diagnose_finnhub.py (or the script's KO/AAPL/MSFT inspection path) and read the printed metric dict keys.
    2. Confirm which of freeCashFlowPerShareAnnual / freeCashFlowPerShareTTM actually appears (or report the real key if different).
    3. Confirm KO's current Price, EPS (TTM), 5Y growth, dividend yield, and the live FRED AAA yield — these become KO_INPUTS in Task 2.
    4. If the FCF key differs from the assumed names, state the correct key so Task 3 uses it.
  </how-to-verify>
  <resume-signal>Type "approved" with the confirmed FCF field name and live KO input values, or describe corrections.</resume-signal>
  <verify>
    <automated>python diagnose_finnhub.py</automated>
  </verify>
  <done>The exact Finnhub FCF field name is confirmed and the live KO input values (price, eps, g, dy, aaa_yield) are recorded for the fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audit Lynch/Graham formulas and commit the KO spot-check fixture (FIX-01, D-05)</name>
  <files>tests/test_valuation_fixture.py, stock_screener.py</files>
  <read_first>
    - stock_screener.py lynch_metrics() (lines 341-417), graham_metrics() (420-458), graham_defensive_score() (461-525), combined_score() (528-537) — these are the formula sites the audit must cover per D-05 (audit ALL Lynch/Graham math vs source defs, not just denominators).
    - RESEARCH.md "Buy Price Bug Analysis" (the canonical formula citations and the diagnosis that the bug is model conservatism, not a sign inversion) and "KO Hand-Verification Fixture" (the step-by-step hand computation and the KO_INPUTS/KO_EXPECTED structure with tolerances).
    - RESEARCH.md Open Question 3: use vanilla Python assert (NOT pytest); do NOT add pytest to requirements.txt.
  </read_first>
  <behavior>
    - Test: calling lynch_metrics(price, eps, g, dy) with the confirmed KO inputs yields Lynch_Category="Slow", Lynch_BuyPrice and Lynch_Discount_Pct matching KO_EXPECTED within tolerance, Lynch_Status="Avoid".
    - Test: calling graham_metrics(price, eps, g, aaa_yield, pb) with the confirmed KO inputs yields Graham_VA, Graham_VB, Graham_FV=min(VA,VB), Graham_Discount_Pct matching KO_EXPECTED within tolerance, Graham_Status="Avoid".
    - Test: the fixture FAILS loudly (assert with a descriptive message) if any computed value drifts outside its tolerance band — this is the bug-detection mechanism.
  </behavior>
  <action>
    Use the live KO inputs confirmed in Task 1 to populate KO_INPUTS, and recompute KO_EXPECTED by hand from those live inputs (do NOT blindly copy RESEARCH.md's June-2026 estimate values — recompute against the confirmed numbers). Create tests/test_valuation_fixture.py importing lynch_metrics and graham_metrics from stock_screener, calling them with KO_INPUTS, and asserting each KO_EXPECTED field within the documented tolerance (plus/minus 0.50 on buy price, 1.0 on Graham values, 10 on Lynch discount %, 5 on Graham discount %). Each assert carries a message naming the field and showing expected-vs-actual.

    Per D-05 / RESEARCH.md recommendation: do NOT change the Lynch/Graham formulas in this phase — the audit's conclusion is that the math is internally consistent and the "visibly wrong" perception is the conservative VB base-P/E of 7 producing low fair values for quality franchises. Instead, ADD a short comment block above graham_metrics() documenting (a) VA = the 1974 rate-adjusted Graham formula (8.5 + 2g) times 4.4/Y, (b) VB = an intentional conservative practitioner variant using base P/E 7 + g (NOT from Graham's book), and (c) that min(VA,VB) deliberately picks the conservative value. Add a one-line comment at the Lynch buy-price site noting FV_GplusD treats Lynch Score = 1.0 as the fair-value anchor. These comments capture the audit so the behavior is not later "fixed" by mistake. Run the fixture; if it fails, the discrepancy IS the bug — diagnose and report which formula deviates before proceeding.

    .gitignore has *.json but this fixture is a .py file — no JSON fixture files are created, so NO new .gitignore exception is needed (RESEARCH.md Pitfall 6).
  </action>
  <verify>
    <automated>python tests/test_valuation_fixture.py</automated>
  </verify>
  <done>tests/test_valuation_fixture.py runs clean (all asserts pass) against the live-confirmed KO inputs; graham_metrics() and the Lynch buy-price site carry audit-documenting comments; no formula was changed; no pytest dependency added.</done>
</task>

<task type="auto">
  <name>Task 3: Route negative inputs to a worst sentinel (D-01) and pass FCF sign through from the Finnhub bundle (D-04)</name>
  <files>stock_screener.py</files>
  <read_first>
    - stock_screener.py process_ticker() growth handling (lines 574-587, especially the g<=0 floor-to-1.0 at 584-586 that RESEARCH.md Pitfall 2 flags), the Lynch/Graham call sites (602-608), and the eps<=0 early return (562-565).
    - stock_screener.py get_combined_data() (250-311) — the return dict at 299-311 is where fcf_per_share is added; get_finnhub_metrics() at 188-204 already fetches the full metric=all bundle (the fh dict).
    - RESEARCH.md "Negative-Input Routing (D-01)" — the RECOMMENDED less-invasive pattern: keep the early-returns in lynch_metrics()/graham_metrics(), intercept the {"error": ...} return in process_ticker(), and set the discount columns to a WORST_DISCOUNT sentinel instead of dropping the row.
    - RESEARCH.md "FCF Field in Finnhub metric=all Bundle" for the exact read pattern (use the field name CONFIRMED in Task 1, not the assumed name if it differed).
    - CONVENTIONS.md: SCREAMING_SNAKE_CASE module constants, space-aligned assignments, Unicode box-drawing dividers, _safe_float() for null-safe reads.
  </read_first>
  <action>
    Per D-01 / D-01b (two distinct paths — negative-but-present vs genuinely-absent; do NOT conflate, RESEARCH.md Pitfall 1):

    (a) Add a loud config constant WORST_DISCOUNT = -999.0 in the existing constant block (near GROWTH_CAP, lines 40-73) with a comment that Plan 02's scorer maps it to sub-score 0.

    (b) Remove the g<=0 floor-to-1.0 block (lines 584-586) per D-01 / RESEARCH.md State-of-the-Art (the floor was a divide-by-zero guard that produced absurd buy prices).

    (c) In process_ticker(), when growth is non-positive OR when lynch_metrics()/graham_metrics() return {"error": ...}, do NOT set row["Error"] and return early for the VALUATION case — instead retain the row and set row["Lynch_Lynch_Discount_Pct"] = WORST_DISCOUNT and row["Graham_Graham_Discount_Pct"] = WORST_DISCOUNT (matching the existing double-prefix key convention produced by the row.update({f"Lynch_{k}": v ...}) pattern), so the stock still ranks but at the bottom. KEEP the genuine no-data early returns (no price line 553-556, no EPS line 562-565) as-is — a missing price/EPS is a true fetch failure (Error row), distinct from a present-but-terrible growth value. Document this distinction in a comment.

    (d) In get_combined_data(): after the fh = get_finnhub_metrics(ticker) fetch, read fcf_per_share = _safe_float(fh.get("<confirmed FCF field>") or fh.get("<confirmed fallback>")) using the field name confirmed in Task 1, and add "fcf_per_share": fcf_per_share, to the return dict (lines 299-311). This reuses the already-fetched bundle — NO new HTTP request (D-04, honors the Phase 5 no-new-fetch constraint). Where the field is absent, _safe_float returns None and Plan 02's trap gate runs on the other 3 inputs (D-01b).

    Surgical-changes rule (CLAUDE.md): touch only the growth-floor block, the negative-input interception, the WORST_DISCOUNT constant, and the get_combined_data return. Do NOT touch combined_score() (CombinedScore is retained per D-02c) or push_to_gsheets (CLN-01 is Phase 4).
  </action>
  <verify>
    <automated>python -c "import ast; src=open('stock_screener.py',encoding='utf-8').read(); assert 'WORST_DISCOUNT' in src; assert 'fcf_per_share' in src; ast.parse(src); print('OK')"</automated>
  </verify>
  <done>WORST_DISCOUNT constant and fcf_per_share return key exist; the g<=0 floor is removed; negative-growth/error rows are retained with WORST_DISCOUNT instead of being dropped; genuine no-price/no-EPS rows still early-return as Error; file parses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Finnhub API → pipeline | Untrusted external JSON (metric=all bundle); read with _safe_float, never eval'd |
| FRED API → pipeline | Untrusted external numeric (AAA yield); already in use |
| Local env → pipeline | API keys read from os.environ (secured in Phase 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information disclosure | diagnose_finnhub.py output | mitigate | Script prints market-data field values only; never print/log the FINNHUB_API_KEY (it is read from os.environ, not echoed). No PII in any field. |
| T-05-02 | Tampering | Finnhub FCF field read | mitigate | Read via _safe_float(fh.get(...)) — malformed/missing values become None and route to D-01b, never crash or inject. |
| T-05-03 | Denial of service | No new fetches | accept | Phase 5 introduces NO new HTTP requests; FCF is read from the already-fetched bundle. No added rate-limit pressure. |
| T-05-SC | Tampering | npm/pip installs | accept | Phase 5 installs ZERO new packages (RESEARCH.md Package Legitimacy Audit: not applicable). No supply-chain surface. |

Note: The public results.json contains only market data — no PII, no secrets. No new secrets or external inputs are introduced in this plan.
</threat_model>

<verification>
- python tests/test_valuation_fixture.py exits 0 (KO fixture passes against live-confirmed inputs).
- python -c "import stock_screener" imports without error (file parses, no broken references).
- WORST_DISCOUNT and fcf_per_share appear in stock_screener.py.
- No formula bodies in lynch_metrics()/graham_metrics() were altered (only comments added); confirm via git diff that the only logic change is the growth-floor removal and the negative-input interception.
- No pytest entry added to requirements.txt.
</verification>

<success_criteria>
- FIX-01: Buy Price root cause is documented (model conservatism per the audit), every Lynch/Graham formula is audited against source definitions, and the committed KO fixture passes.
- FIX-02: Lynch_Discount_Pct / Graham_Discount_Pct read as sane (match the audited formula output) — this is the hard gate; Plan 02 must not start until this passes.
- D-01 groundwork: negative/zero EPS or growth no longer drops the ticker; it is retained with WORST_DISCOUNT.
- D-04 groundwork: fcf_per_share is available in get_combined_data() output with no new fetch.
</success_criteria>

<output>
Create .planning/phases/05-score-foundation-public-top-n/05-01-SUMMARY.md when done. Record: confirmed FCF field name, the live KO inputs used, the audit conclusion, and any formula discrepancy the fixture caught.
</output>
