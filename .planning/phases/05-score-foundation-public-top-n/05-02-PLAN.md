---
phase: 05-score-foundation-public-top-n
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - stock_screener.py
  - tests/test_scoring.py
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03, SCORE-04, SCORE-05, SCORE-06, SCORE-07, SCORE-08, TRAP-01]
must_haves:
  truths:
    - "Each scored row carries an OverallScore (0-100) decomposing into score_value/score_quality/score_growth/score_safety pillar sub-scores"
    - "OverallScore is the primary descending sort key in run_screener (CombinedScore retained as a column)"
    - "Each raw metric maps to its sub-score through version-controlled piecewise-linear absolute-threshold bands (not cross-sectional ranks), after both-tail winsorization"
    - "Missing metrics are averaged over present metrics within a pillar with a coverage_pct flag; a row with all Safety inputs missing is treated as unknown, never safe"
    - "A negative/calc-breaking input (WORST_DISCOUNT sentinel, negative D/E, negative growth) maps to the worst sub-score for that metric, so the stock still ranks but at the bottom"
    - "An interim value-trap gate (debt/equity, current ratio, EPS stability, negative FCF) sets is_trap and floors the Safety sub-score when tripped; the row is still shown"
    - "results.json carries both flat pillar columns and a nested scores object per row"
  artifacts:
    - path: "stock_screener.py"
      provides: "SCORE_*/TRAP_* config constants, piecewise/winsorize/avg_present helpers, overall_score(), trap_gate(), row-assembly wiring, sort swap, nested-scores write_json"
      contains: "def overall_score"
    - path: "tests/test_scoring.py"
      provides: "Unit tests for piecewise bands, winsorization, avg-over-present, negative-input worst-score, trap gate, and missing-Safety-is-unknown"
      contains: "def test_"
  key_links:
    - from: "process_ticker() row assembly"
      to: "overall_score() and trap_gate()"
      via: "scores = overall_score(...); is_trap, coverage = trap_gate(...); row merges flat columns"
      pattern: "overall_score\\("
    - from: "run_screener()"
      to: "OverallScore sort"
      via: "df.sort_values('OverallScore', ascending=False, na_position='last')"
      pattern: "sort_values\\(\"OverallScore\""
    - from: "write_json()"
      to: "nested scores object"
      via: "row['scores'] = {overall, value, quality, growth, safety, coverage_pct, trap}"
      pattern: "\"scores\""
---

<objective>
Build the 4-pillar absolute OverallScore engine (SCORE-01..08) and the interim value-trap gate (TRAP-01) on top of the corrected discount fields from Plan 01. All weights, band thresholds, and winsorization bounds live as loud version-controlled config constants. The composite is built entirely from metrics that already exist — no new data fetches.

Depends on Plan 01: the Value pillar consumes the audited Lynch_Discount_Pct / Graham_Discount_Pct, negative inputs already arrive as the WORST_DISCOUNT sentinel, and the trap gate reads the fcf_per_share field Plan 01 added. Do NOT begin this plan until Plan 01's FIX-02 gate has passed.

Purpose: Replace the price-only CombinedScore with a richer, absolute, glitch-resistant ranking that catches cheap-but-dying stocks before any public list ships.
Output: SCORE_*/TRAP_* constants, pure scoring helpers (piecewise/winsorize/avg_present), overall_score(), trap_gate(), wired into process_ticker()/run_screener()/write_json(), plus a unit-test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-score-foundation-public-top-n/05-CONTEXT.md
@.planning/phases/05-score-foundation-public-top-n/05-RESEARCH.md
@.planning/phases/05-score-foundation-public-top-n/05-01-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
New symbols this plan introduces (Plan 03 frontend and the verifier depend on these JSON field names):

| Symbol / Key | Kind | Defined in | Notes |
|---|---|---|---|
| SCORE_DISC_WIN_LO / SCORE_DISC_WIN_HI / SCORE_DISC_BANDS | constants | stock_screener.py config block | Value-discount winsorization + piecewise bands |
| SCORE_DEF_BANDS | constant | stock_screener.py | DefensiveScore (0-8) bands |
| SCORE_DE_WIN_HI / SCORE_DE_BANDS | constants | stock_screener.py | Debt/equity bands (inverted: lower = better) |
| SCORE_CR_WIN_HI / SCORE_CR_BANDS | constants | stock_screener.py | Current-ratio bands |
| SCORE_G_BANDS | constant | stock_screener.py | Growth-level bands |
| SCORE_GSTAB_BANDS | constant | stock_screener.py | Growth-stability bands |
| SCORE_SAFETY_TRAP_PENALTY / SCORE_SAFETY_NOTRAP_BASE | constants | stock_screener.py | Safety floor on trap; non-trapped baseline |
| SCORE_AAA_REFERENCE | constant | stock_screener.py | Graham 1963 reference yield for rate-relativization |
| PILLAR_WEIGHTS | constant | stock_screener.py | {value:0.35, quality:0.30, growth:0.20, safety:0.15} (D-02) |
| TRAP_MAX_DE / TRAP_MIN_CR | constants | stock_screener.py | Trap-gate thresholds |
| _piecewise_score(value, bands) | helper | stock_screener.py | Linear interpolation to [0,100] |
| _winsorize(value, lo, hi) | helper | stock_screener.py | Both-tail clamp |
| _avg_present(values) | helper | stock_screener.py | Average over non-None; None if all absent |
| trap_gate(debt_equity, current_ratio, eps_stability, fcf_per_share) | function | stock_screener.py | Returns (is_trap, coverage_fraction) |
| overall_score(...) | function | stock_screener.py | Returns dict: overall, value, quality, growth, safety, coverage_pct |
| OverallScore / score_value / score_quality / score_growth / score_safety / is_trap / coverage_pct | flat row columns | process_ticker() | Additive — existing keys untouched |
| scores | nested JSON object | write_json() | {overall, value, quality, growth, safety, coverage_pct, trap} |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config constants + pure scoring helpers + trap gate (with unit tests)</name>
  <files>stock_screener.py, tests/test_scoring.py</files>
  <read_first>
    - stock_screener.py config block (lines 40-73) — add the SCORE_*/TRAP_*/PILLAR_WEIGHTS constants here in the same loud SCREAMING_SNAKE_CASE, space-aligned style (D-02b, CONVENTIONS.md).
    - RESEARCH.md "Piecewise-Linear Band Design" — the exact proposed band constant values for discount/defensive/debt-equity/current-ratio/growth/growth-stability and the _piecewise_score pattern in "Code Examples".
    - RESEARCH.md "Winsorization Strategy" (the bounds table and _winsorize pattern) and "avg_present Pattern".
    - RESEARCH.md "Value-Trap Gate (TRAP-01)" — trap_gate() logic and thresholds (TRAP_MAX_DE=2.0, TRAP_MIN_CR=1.0, eps_stability==0 trips, negative FCF trips; coverage = present/4).
    - RESEARCH.md "Rate-relativization for yield-based thresholds" (SCORE_AAA_REFERENCE=4.4; scale discount band breakpoints by SCORE_AAA_REFERENCE/aaa_yield).
  </read_first>
  <behavior>
    - _piecewise_score: a value below the first band returns score_lo of the first band; above the last returns score_hi of the last; interior values interpolate linearly; t is clamped to [0,1].
    - _winsorize: clamps both tails to [lo, hi].
    - _avg_present: averages non-None values; returns None when all are None.
    - trap_gate: returns is_trap=True if ANY present input trips (D/E > TRAP_MAX_DE, current ratio < TRAP_MIN_CR, eps_stability == 0, fcf_per_share < 0); coverage_fraction = count_present/4; with all inputs None returns (False, 0.0) and the caller treats Safety as unknown (D-01b), never safe.
    - Negative debt/equity (negative equity) → worst sub-score path, not the winsorize path (D-01).
  </behavior>
  <action>
    Add the config constants (per D-02b, all loud and tunable with a comment that they have NO empirical anchor yet and are monitored in Phase 7 stats.html). Use the band values proposed in RESEARCH.md as the first-pass constants; exact breakpoints are Claude's discretion within the loud-constant rule. Add PILLAR_WEIGHTS = {"value":0.35,"quality":0.30,"growth":0.20,"safety":0.15} per D-02 (~35/30/20/15).

    Implement the three pure helpers (_piecewise_score, _winsorize, _avg_present) and trap_gate() exactly per the RESEARCH.md patterns, in a new "STEP — SCORING" section using the Unicode box-drawing divider convention. trap_gate inputs are debt_equity, current_ratio, eps_stability (the 0/1 EPS_Stability check from graham_defensive_score), and fcf_per_share (from Plan 01's get_combined_data field) — D-04.

    Write tests/test_scoring.py (vanilla assert, no pytest dependency) covering: piecewise below/at/above band edges and an interior interpolation; winsorize both tails; avg_present with a None mixed in and the all-None case; trap_gate tripping on each of the 4 inputs individually and the all-None unknown case; coverage_fraction arithmetic. These are deterministic input→output checks.

    Do NOT compute pillars or OverallScore yet — that is Task 2. Keep helpers side-effect free.
  </action>
  <verify>
    <automated>python tests/test_scoring.py</automated>
  </verify>
  <done>SCORE_*/TRAP_*/PILLAR_WEIGHTS constants exist in the config block; _piecewise_score/_winsorize/_avg_present/trap_gate implemented; tests/test_scoring.py passes; no pytest added to requirements.txt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: overall_score() — pillar aggregation, negative-input worst-score, missing-data + coverage, Safety from trap gate</name>
  <files>stock_screener.py, tests/test_scoring.py</files>
  <read_first>
    - RESEARCH.md "Scoring Engine Design" (overall_score signature and Negative-Input Routing), "Value Pillar Grouping (SCORE-07)" (two-level average: discount-group then pillar), "Missing-Data Rule (SCORE-04 / D-01b)", and the Safety sub-score rules under "Safety: Trap gate result" (tripped → SCORE_SAFETY_TRAP_PENALTY=0; non-trapped → SCORE_SAFETY_NOTRAP_BASE * coverage_fraction).
    - RESEARCH.md Pitfall 1 (D-01 vs D-01b conflation — negative-but-present floors to 0; genuinely-None is averaged-over-present) — the two paths must stay distinct.
    - 05-CONTEXT.md "Phase 5 pillar inputs": VALUE = Lynch+Graham discount grouped as ONE discount sub-factor (SCORE-07); QUALITY = DefensiveScore + debt_equity + current_ratio; GROWTH = growth g + growth-stability; SAFETY = trap gate. debt_equity & current_ratio deliberately feed BOTH Quality and Safety — document, do not "fix".
    - The WORST_DISCOUNT sentinel from Plan 01 (05-01-SUMMARY.md) — overall_score must map a discount equal to WORST_DISCOUNT (and any negative D/E or non-positive growth flagged as negative-but-present) to sub-score 0, NOT to the winsorize path.
  </read_first>
  <behavior>
    - A row with strong discounts, high DefensiveScore, low D/E, healthy current ratio, good growth+stability, and no trap returns a high OverallScore (>= ~65) decomposing into all four pillars.
    - A row whose discount == WORST_DISCOUNT (negative-input sentinel) gets a Value sub-score of 0 (worst), so OverallScore is dragged down — the stock still ranks but at the bottom (D-01).
    - A row with all Safety/trap inputs None yields a Safety sub-score that reflects "unknown" via coverage (never the full non-trapped baseline) — missing Safety is never treated as safe (D-01b).
    - A tripped trap gate floors Safety to SCORE_SAFETY_TRAP_PENALTY (0) regardless of other Safety signals (D-03).
    - coverage_pct reflects the fraction of expected sub-scores that were present across the row.
    - When an entire pillar's inputs are all None, that pillar contributes None and OverallScore averages over the present pillars using PILLAR_WEIGHTS renormalized to present pillars.
  </behavior>
  <action>
    Implement overall_score(lynch_discount, graham_discount, defensive_score, debt_equity, current_ratio, growth_g, growth_stability, is_trap, coverage_fraction, aaa_yield) returning {overall, value, quality, growth, safety, coverage_pct}.

    Value (SCORE-07): winsorize each discount, rate-relativize the discount band breakpoints by SCORE_AAA_REFERENCE/aaa_yield, piecewise-score each, then _avg_present the two into the discount sub-group, then _avg_present the sub-groups into score_value (two-level structure established now even with one group, so Phase 6 adds a second group trivially). A discount equal to WORST_DISCOUNT → sub-score 0 (D-01 path), checked BEFORE winsorize.

    Quality: piecewise-score DefensiveScore (SCORE_DEF_BANDS), debt_equity (SCORE_DE_BANDS, inverted; negative D/E → 0 via D-01), current_ratio (SCORE_CR_BANDS); _avg_present them into score_quality.

    Growth: piecewise-score growth g (SCORE_G_BANDS; non-positive g flagged negative-but-present → 0 via D-01) and growth_stability (SCORE_GSTAB_BANDS; None when fewer than 3 EPS years → D-01b); _avg_present into score_growth.

    Safety: if is_trap → SCORE_SAFETY_TRAP_PENALTY (0, D-03); elif coverage_fraction == 0 (all trap inputs missing) → None handled as unknown (NOT safe, D-01b); else SCORE_SAFETY_NOTRAP_BASE * coverage_fraction.

    OverallScore: weighted average of present pillars using PILLAR_WEIGHTS, renormalizing weights over present pillars (avg-over-present at the pillar level, D-02). coverage_pct = present sub-scores / total expected, expressed 0-100.

    Add unit tests to tests/test_scoring.py for: the high-quality row, the WORST_DISCOUNT row scoring Value=0, the all-Safety-missing unknown case, the tripped-trap Safety=0 case, and a pillar-all-None renormalization case.

    Document in a comment the intentional double-use of debt_equity/current_ratio in both Quality and Safety (per 05-CONTEXT.md — so it is not later "cleaned up").
  </action>
  <verify>
    <automated>python tests/test_scoring.py</automated>
  </verify>
  <done>overall_score() implemented with the two-level Value grouping, D-01 worst-score path distinct from D-01b averaging, trap-floored Safety, missing-Safety-as-unknown, and pillar-weight renormalization; all tests/test_scoring.py cases pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire scores into row assembly, swap the sort key, and emit the nested scores object</name>
  <files>stock_screener.py</files>
  <read_first>
    - stock_screener.py process_ticker() row assembly (lines 602-635) — where Lynch/Graham/defensive/CombinedScore columns are set; add the trap-gate call and overall_score call here, then merge the flat score columns.
    - stock_screener.py run_screener() sort (lines 648-651) — change the sort key from CombinedScore to OverallScore (SCORE-08 / D-02c) while KEEPING the CombinedScore column.
    - stock_screener.py write_json() (lines 661-677) — build the nested scores object post-serialization per RESEARCH.md "Write nested scores object in write_json()" / Pitfall 3 (preferred: construct after json.loads(df.to_json(...)), NOT a dict-valued DataFrame column).
    - RESEARCH.md "JSON Schema Extension (SCORE-05)" for the exact flat column names and nested object shape (additive — existing keys, including the Lynch_Lynch_/Graham_Graham_ double-prefix wart, untouched).
  </read_first>
  <action>
    In process_ticker(), after the defensive score and CombinedScore are computed: call is_trap, coverage = trap_gate(fund["debt_equity"], fund["current_ratio"], ds.get("EPS_Stability"), fund["fcf_per_share"]). Compute a growth_stability value from the existing annual_eps history (Claude's discretion per CONTEXT.md; RESEARCH.md Open Question 2 recommends fraction of years with positive EPS and <20% YoY decline, None when fewer than 3 years). Call scores = overall_score(...) passing the audited discount fields (note the double-prefix keys lm.get("Lynch_Discount_Pct") become row "Lynch_Lynch_Discount_Pct"; pass the raw lm/gm values), defensive_score, debt_equity, current_ratio, g, growth_stability, is_trap, coverage, aaa_yield. Merge flat columns: row["OverallScore"], row["score_value"], row["score_quality"], row["score_growth"], row["score_safety"] = scores fields; row["is_trap"] = is_trap; row["coverage_pct"] = scores["coverage_pct"]. Keep all existing columns (additive, D-02c).

    IMPORTANT: rows that were retained with WORST_DISCOUNT in Plan 01 (negative-input rows) must still flow through overall_score so they receive Value=0 and rank at the bottom — confirm the negative-input rows reach this code path rather than early-returning.

    In run_screener(): change df.sort_values("CombinedScore", ...) to df.sort_values("OverallScore", ascending=False, na_position="last"); guard on "OverallScore" in df.columns. Do NOT drop the CombinedScore column.

    In write_json(): after rows = json.loads(df.to_json(orient="records")), loop each row and set row["scores"] = {"overall": row.get("OverallScore"), "value": row.get("score_value"), "quality": row.get("score_quality"), "growth": row.get("score_growth"), "safety": row.get("score_safety"), "coverage_pct": row.get("coverage_pct"), "trap": row.get("is_trap", False)} (Pitfall 3 preferred approach — avoids pandas dict-column edge cases). Keep the min-row guard (abort if < 100 rows) and compact encoding (separators) unchanged (PY-03/PY-04, CLAUDE.md gotcha).
  </action>
  <verify>
    <automated>python -c "import ast; src=open('stock_screener.py',encoding='utf-8').read(); assert 'overall_score(' in src; assert 'sort_values(\"OverallScore\"' in src; assert '\"scores\"' in src; assert 'is_trap' in src; ast.parse(src); print('OK')"</automated>
  </verify>
  <done>process_ticker() emits OverallScore + 4 pillar columns + is_trap + coverage_pct; run_screener() sorts by OverallScore (CombinedScore retained); write_json() emits the nested scores object; min-row guard and compact encoding intact; file parses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Finnhub/yfinance → scoring | Untrusted external numerics flow into scoring math; clamped via _winsorize and routed by D-01/D-01b |
| Pipeline → results.json | Scores written to a public file served by GitHub Pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | Glitch input dominating a sub-score | mitigate | SCORE-03 both-tail winsorization before piecewise mapping bounds every metric; one glitch (e.g. 1179% growth) cannot dominate (g already GROWTH_CAP-capped + winsorized). |
| T-05-05 | Repudiation / integrity | Missing Safety silently scored as safe | mitigate | D-01b: all-None trap inputs yield unknown (never the full non-trapped baseline); negative inputs floor to 0 (D-01). |
| T-05-06 | Information disclosure | results.json contents | accept | Output is market data only — no PII, no secrets. Public exposure is by design. |
| T-05-SC | Tampering | npm/pip installs | accept | Zero new packages (pure stdlib + existing deps). No supply-chain surface. |

No new secrets or external inputs introduced. All scoring is pure computation over already-fetched data.
</threat_model>

<verification>
- python tests/test_scoring.py exits 0 (helpers, trap gate, and overall_score behaviors all pass).
- python -c "import stock_screener" imports clean.
- overall_score(, sort_values("OverallScore", and "scores" appear in stock_screener.py.
- A negative-input row (WORST_DISCOUNT) produces Value sub-score 0 and ranks at the bottom (covered by test_scoring).
- CombinedScore column still present (additive schema, D-02c) — confirm via git diff that it is not removed.
- requirements.txt unchanged (no pytest).
</verification>

<success_criteria>
- SCORE-01..08 satisfied: 4-pillar absolute OverallScore (0-100) with pillar decomposition in flat columns + nested scores object, piecewise absolute-threshold bands (not ranks), both-tail winsorization, average-over-present + coverage flag with missing-Safety-as-unknown, version-controlled weights/thresholds with AAA rate-relativization, grouped Value metrics, and OverallScore as the primary sort key.
- TRAP-01 satisfied: interim trap gate computed from debt/equity, current ratio, EPS stability, and negative FCF; tripped gate floors Safety and sets is_trap; row still shown.
</success_criteria>

<output>
Create .planning/phases/05-score-foundation-public-top-n/05-02-SUMMARY.md when done. Record: the final constant values chosen, the growth-stability formula used, the exact flat + nested JSON field names emitted, and a sample scored row for KO.
</output>
