---
phase: 05-score-foundation-public-top-n
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - docs/app.js
  - docs/top.html
  - docs/index.html
  - docs/methodology.html
  - docs/style.css
autonomous: false
requirements: [PAGE-01, PAGE-03, PAGE-04, TRAP-02, SCORE-05, SCORE-08]
must_haves:
  truths:
    - "docs/app.js exists and exposes SIGNAL_COLORS, COLOR_STYLES, makeSignalFormatter, numFmt, pctFmt, updateFreshnessUI, and a new array-driven buildNav(activePage)"
    - "docs/top.html loads the same results.json, sorts by OverallScore desc, and renders ranked cards with a 10/25 toggle"
    - "Every top.html card shows the OverallScore badge (color-coded), four pillar sub-score chips, headline Lynch/Graham/Defensive signals, and a TRAP badge when is_trap is true"
    - "The site nav (Dashboard, Top Picks, Methodology) is generated by buildNav() and rendered identically on index.html, top.html, and methodology.html"
    - "index.html loads app.js, shows the new OverallScore/pillar/Trap columns, and its initial sort is OverallScore descending"
  artifacts:
    - path: "docs/app.js"
      provides: "Shared fetch/format/color/freshness primitives + buildNav"
      contains: "function buildNav"
    - path: "docs/top.html"
      provides: "Top 10/25 ranked-card page driven by OverallScore"
      contains: "renderTopN"
    - path: "docs/style.css"
      provides: "Card/badge/chip styles (.top-card, .score-badge, .trap-badge, .pillar-chip)"
      contains: ".top-card"
  key_links:
    - from: "docs/top.html"
      to: "data/results.json"
      via: "fetch('data/results.json?v=' + Date.now())"
      pattern: "results\\.json\\?v="
    - from: "docs/top.html and index.html and methodology.html"
      to: "buildNav() in app.js"
      via: "<script src=\"app.js\"> then buildNav(activePage)"
      pattern: "buildNav\\("
    - from: "top.html card"
      to: "row.is_trap"
      via: "render TRAP badge when is_trap is true"
      pattern: "is_trap"
---

<objective>
Ship the public Top-N page and the shared frontend module: extract the reusable primitives from index.html into docs/app.js (PAGE-03), add an array-driven buildNav() and wire the 3-link nav across all pages (PAGE-04), build docs/top.html with the approved ranked-card layout and 10/25 toggle (PAGE-01), surface the value-trap badge on each card (TRAP-02), and update index.html to load app.js, show the new score columns, and sort by OverallScore (SCORE-05/SCORE-08 UI surfacing).

Depends on Plan 02: top.html and the new index.html columns read the OverallScore / score_value / score_quality / score_growth / score_safety / is_trap fields and the nested scores object that Plan 02 added to results.json. Do NOT begin until Plan 02 has produced the new JSON schema.

Purpose: Make the new absolute scoring publicly browsable and put a value-trap warning in front of users before they act on a cheap-looking pick.
Output: docs/app.js (new), docs/top.html (new), and modified docs/index.html, docs/methodology.html, docs/style.css.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-score-foundation-public-top-n/05-CONTEXT.md
@.planning/phases/05-score-foundation-public-top-n/05-RESEARCH.md
@.planning/phases/05-score-foundation-public-top-n/05-UI-SPEC.md
@.planning/phases/05-score-foundation-public-top-n/05-02-SUMMARY.md
@docs/index.html
@docs/style.css
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
| Symbol / File | Kind | Defined in | Notes |
|---|---|---|---|
| docs/app.js | new file | this plan | Shared module-level var/function declarations (no ES modules) |
| NAV_ENTRIES | array constant | app.js | Array-driven nav; adding a 4th entry (Stats, Phase 7) is one line |
| buildNav(activePage) | function | app.js | Sets innerHTML of nav.main-nav; activePage in {"dashboard","top","methodology"} |
| SIGNAL_COLORS, COLOR_STYLES, makeSignalFormatter, numFmt, pctFmt, updateFreshnessUI | moved | app.js | Cut from index.html inline script verbatim (double-prefix keys preserved) |
| docs/top.html | new file | this plan | Top 10/25 ranked-card page |
| renderTopN(rows, n) | function | top.html inline script | Sorts/slices/renders cards |
| .top-card / .score-badge / .pillar-chip / .trap-badge | CSS classes | style.css | Per UI-SPEC measurements |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract docs/app.js (shared primitives + buildNav) and rewire index.html + methodology.html to use it</name>
  <files>docs/app.js, docs/index.html, docs/methodology.html</files>
  <read_first>
    - docs/index.html inline script: SIGNAL_COLORS (lines 80-102), COLOR_STYLES (104-108), makeSignalFormatter (111-125), numFmt (128-135), pctFmt (138-142), updateFreshnessUI (240-251). These move to app.js VERBATIM — including the double-prefix keys Lynch_Lynch_Status / Graham_Graham_Status (RESEARCH.md Pitfall 5 — do NOT "fix" them).
    - docs/index.html nav markup (lines 29-32, currently 2 links) and the Tabulator script tag (line 70) + inline <script> open (line 72).
    - docs/methodology.html: nav markup (lines 14-17, currently 2 links, no app.js loaded) and the existing inline <script> at the end (tab logic) — app.js loads before it.
    - RESEARCH.md "app.js Extraction (PAGE-03)" (extraction table + buildNav() spec + loading order) and "Pitfall 4" (load app.js after Tabulator, before the page-specific script).
    - 05-UI-SPEC.md buildNav() contract (array-driven, activePage adds class "nav-link active" + aria-current="page", Stats NOT added in Phase 5 but the array makes it trivial).
  </read_first>
  <action>
    Create docs/app.js with module-level var/function declarations (no const, no ES modules — match the existing convention so it loads without a module system). Move SIGNAL_COLORS, COLOR_STYLES, makeSignalFormatter, numFmt, pctFmt, updateFreshnessUI out of index.html into app.js unchanged. Add NAV_ENTRIES (Dashboard→index.html key "dashboard", Top Picks→top.html key "top", Methodology→methodology.html key "methodology") and buildNav(activePage) per the RESEARCH.md spec — array-driven .map() that sets nav.main-nav innerHTML, applies "active" class + aria-current="page" to the active entry. Stats is NOT in the array this phase (PAGE-04 nuance per CONTEXT.md Claude's Discretion — a live Stats link would 404; the array makes Phase 7's addition one line).

    In index.html: add <script src="app.js"></script> after the Tabulator CDN tag (line 70) and before the inline page script (line 72); remove the now-extracted functions/constants from the inline script (they are defined in app.js); replace the hardcoded 2-link nav (lines 29-32) with an empty <nav class="main-nav" aria-label="Site navigation"></nav> and call buildNav("dashboard") inside the fetch().then() handler (after app.js is loaded). Verify makeSignalFormatter/numFmt/pctFmt references in buildColumns() still resolve (they now come from app.js).

    In methodology.html: add <script src="app.js"></script> before its existing inline <script>; replace the hardcoded 2-link nav (lines 14-17) with the empty <nav class="main-nav" aria-label="Site navigation"></nav> and call buildNav("methodology") (at DOMContentLoaded or top of the existing inline script). methodology.html does not fetch results.json, so buildNav runs on load.

    Surgical rule: do not restyle or reorder anything else; the only index.html logic change is the extraction + nav swap + sort key (sort key handled in Task 3 to keep this task focused, OR do it here — keep within this file set). Keep the double-prefix SIGNAL_COLORS keys.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const a=fs.readFileSync('docs/app.js','utf8');['SIGNAL_COLORS','COLOR_STYLES','makeSignalFormatter','numFmt','pctFmt','updateFreshnessUI','buildNav','NAV_ENTRIES'].forEach(s=>{if(!a.includes(s))throw new Error('app.js missing '+s)});const i=fs.readFileSync('docs/index.html','utf8');if(!i.includes('app.js'))throw new Error('index.html does not load app.js');const m=fs.readFileSync('docs/methodology.html','utf8');if(!m.includes('buildNav(\"methodology\")'))throw new Error('methodology missing buildNav');console.log('OK')"</automated>
  </verify>
  <done>docs/app.js exists with all 6 extracted primitives + NAV_ENTRIES + buildNav; index.html and methodology.html load app.js and render their nav via buildNav(); double-prefix SIGNAL_COLORS keys preserved; index.html dashboard still functions (no undefined-reference errors).</done>
</task>

<task type="auto">
  <name>Task 2: Build docs/top.html (ranked cards, 10/25 toggle, TRAP badge) and add card/badge/chip CSS</name>
  <files>docs/top.html, docs/style.css</files>
  <read_first>
    - 05-UI-SPEC.md in full — the authoritative contract: page structure (stale-banner, site-header, toolbar with Top 10 / Top 25 pill buttons, main#top-container), ranked-card layout (3 rows: identity+score, pillar chips, headline signals), exact card/badge/chip CSS measurements (card bg #3b4252, border 1px #4c566a, radius 6px, padding 12px 16px, margin-bottom 8px, max-width 860px centered; chip bg #434c5e text #d8dee9 padding 4px 8px radius 4px; OverallScore badge thresholds >=70 green / 40-69 yellow / <40 red; TRAP badge bg #bf616a text #eceff4), the copywriting contract (title, toggle labels, empty/error states), the interaction contract (toggle re-slices client-side no refetch), and accessibility notes.
    - RESEARCH.md "top.html Implementation Notes" — data flow (fetch?v=Date.now(), filter !Error, sort OverallScore desc, slice top N), card fields read flat columns r.OverallScore/r.score_value/.../r.is_trap/r.Price/r.Lynch_Lynch_Status/r.Graham_Graham_Status/r.DefensiveLabel, and the 10/25 toggle (update currentN, re-slice already-sorted array, re-render — no refetch).
    - docs/index.html for the Finviz ticker-link pattern (line 152) and the cache-busted fetch pattern (line 265) to match exactly.
    - docs/style.css existing classes to reuse without redefining: .stale-banner, .site-header, .site-title, .main-nav, .nav-link, .freshness-badge, .toolbar, .btn-pill, .toolbar-separator.
  </read_first>
  <action>
    Create docs/top.html following the UI-SPEC page structure exactly: load style.css and Tabulator CSS in head (Tabulator CSS only if needed for shared tokens — top.html uses cards, not a Tabulator table, so it may omit the Tabulator JS/CSS; per UI-SPEC Registry Safety it loads only style.css + app.js + (optionally) Tabulator — prefer NOT loading Tabulator since no table is used). Title: "Top Picks — Lynch & Graham Screener". Reuse .stale-banner, .site-header, .main-nav, .freshness-badge, .toolbar, .btn-pill classes. Toolbar holds Top 10 (active default) and Top 25 pill buttons with aria-pressed.

    Inline <script> (after <script src="app.js">): fetch("data/results.json?v=" + Date.now()), on success call updateFreshnessUI(data.generated_at) and buildNav("top"), filter out rows where Error is non-null, sort by (b.OverallScore||0)-(a.OverallScore||0) desc, store the sorted array, and renderTopN(rows, currentN) with currentN=10. renderTopN renders one .top-card per row: Row 1 = #rank, Finviz-linked ticker (same pattern as dashboard), Price (2dp), OverallScore badge (color via the >=70/40-69/<40 thresholds, inline style like makeSignalFormatter), and a .trap-badge "TRAP" only when r.is_trap is true; Row 2 = four .pillar-chip "Value: N  Quality: N  Growth: N  Safety: N" (numFmt-style, 1dp, em-dash for null); Row 3 = Lynch/Graham/Defensive signals using the SIGNAL_COLORS/COLOR_STYLES coloring from app.js (read r.Lynch_Lynch_Status, r.Graham_Graham_Status, r.DefensiveLabel — the double-prefix keys). Wire the 10/25 toggle to update currentN, toggle active class + aria-pressed, and re-slice+re-render WITHOUT refetching. Handle fetch error (freshness badge "Error loading data" + empty-state copy) and empty-after-filter (empty-state copy) per the UI-SPEC copy.

    Add to docs/style.css (append, do not modify existing rules): .top-card, .score-badge, .pillar-chip, .trap-badge, and any rank-number / card-row layout rules, using the exact measurements from UI-SPEC. No new CSS file; all styles live in style.css.

    Cache-bust the fetch with ?v=Date.now() (CLAUDE.md gotcha). Do not introduce new CDN dependencies (UI-SPEC Registry Safety).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('docs/top.html','utf8');['results.json?v=','buildNav(\"top\")','renderTopN','is_trap','Top 10','Top 25','OverallScore'].forEach(s=>{if(!t.includes(s))throw new Error('top.html missing '+s)});const c=fs.readFileSync('docs/style.css','utf8');['.top-card','.pillar-chip','.trap-badge'].forEach(s=>{if(!c.includes(s))throw new Error('style.css missing '+s)});console.log('OK')"</automated>
  </verify>
  <done>docs/top.html renders Top 10/25 ranked cards sorted by OverallScore with pillar chips, headline signals, color-coded OverallScore badge, and a TRAP badge when is_trap; the 10/25 toggle re-slices client-side; nav and freshness come from app.js; card/badge/chip CSS appended to style.css; cache-busted fetch; no new CDN.</done>
</task>

<task type="auto">
  <name>Task 3: Add OverallScore/pillar/Trap columns to the dashboard and switch its sort key</name>
  <files>docs/index.html</files>
  <read_first>
    - docs/index.html buildColumns() (lines 145-223) — add the new columns in the appropriate sections; numFmt(1) for scores, boolean display for Trap.
    - docs/index.html SUMMARY_COLS (lines 226-232) — add OverallScore and is_trap.
    - docs/index.html initialSort (line 281) — change from CombinedScore to OverallScore.
    - 05-UI-SPEC.md "Updated index.html (dashboard) additions" table — exact column titles/fields/formatters/filters: Overall Score=OverallScore numFmt(1), Value=score_value, Quality=score_quality, Growth=score_growth, Safety=score_safety, Trap?=is_trap (boolean display "Yes"/"—", boolean dropdown filter). SUMMARY_COLS add OverallScore + is_trap. Keep CombinedScore column (retained, non-summary).
  </read_first>
  <action>
    In buildColumns(): add columns Overall Score (field OverallScore, numFmt(1), numeric >= filter), Value (score_value), Quality (score_quality), Growth (score_growth), Safety (score_safety) all numFmt(1) with numeric filters, and Trap? (field is_trap) with a formatter showing "Yes" when true and "—" otherwise plus a boolean/list dropdown filter. Place OverallScore near the front (it is the new primary signal) and the pillar columns grouped together; place Trap? near the signal columns. Add OverallScore and is_trap to SUMMARY_COLS. Change initialSort to [{ column: "OverallScore", dir: "desc" }] (SCORE-08). Keep the existing CombinedScore "Score" column as a retained non-summary column (additive schema, D-02c — do not remove it).

    Surgical rule: only add columns, update SUMMARY_COLS, and change the sort key. Do not restructure buildColumns or touch unrelated columns. If Task 1 already moved the nav/app.js load, do not duplicate it here.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const i=fs.readFileSync('docs/index.html','utf8');['OverallScore','score_value','score_quality','score_growth','score_safety','is_trap'].forEach(s=>{if(!i.includes(s))throw new Error('index.html missing '+s)});if(!i.includes('column: \"OverallScore\", dir: \"desc\"'))throw new Error('initialSort not switched to OverallScore');if(!i.includes('CombinedScore'))throw new Error('CombinedScore column was removed (must be retained)');console.log('OK')"</automated>
  </verify>
  <done>Dashboard shows OverallScore + 4 pillar columns + Trap?; SUMMARY_COLS includes OverallScore and is_trap; initialSort is OverallScore desc; CombinedScore column retained.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification of top.html, the dashboard, and nav across all pages</name>
  <what-built>
    docs/top.html (Top 10/25 ranked cards with OverallScore badge, pillar chips, headline signals, TRAP badge), the updated dashboard (new score columns + OverallScore sort), and the shared 3-link nav generated by buildNav() on all three pages. Claude has already verified file structure via the automated checks above; this checkpoint confirms the rendered result matches the UI-SPEC contract.
  </what-built>
  <how-to-verify>
    1. Serve docs/ locally (e.g. `python -m http.server 8000 --directory docs`) and open http://localhost:8000/top.html.
    2. Confirm: ranked cards appear sorted by OverallScore desc; OverallScore badge is green (>=70) / yellow (40-69) / red (<40); the four pillar chips show Value/Quality/Growth/Safety; Lynch/Graham/Defensive signals are color-coded matching the dashboard; a TRAP badge appears on any trapped row.
    3. Click Top 25 — the list expands to 25 without a page reload; click Top 10 — it contracts. aria-pressed updates.
    4. Open index.html — Overall Score, Value/Quality/Growth/Safety, and Trap? columns appear; the table is sorted by Overall Score desc; CombinedScore "Score" column still present.
    5. Confirm the nav (Dashboard, Top Picks, Methodology) appears identically on index.html, top.html, and methodology.html, with the current page highlighted; the links navigate correctly and none 404 (no Stats link this phase).
    6. Confirm the freshness badge and stale banner behave as on the dashboard.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/behavioral issues to fix.</resume-signal>
  <verify>
    <automated>node -e "['docs/app.js','docs/top.html'].forEach(f=>require('fs').accessSync(f));console.log('files present')"</automated>
  </verify>
  <done>top.html, the dashboard, and the shared nav render correctly per the UI-SPEC and the user has approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| results.json → browser | Public JSON fetched and rendered client-side; values inserted into DOM |
| User browser → static pages | No server; no user input persisted; read-only viewing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering (XSS) | Card rendering inserts ticker/signal values into innerHTML | mitigate | Values come from the project's own results.json (market data, controlled pipeline), not user input. Ticker is used in a Finviz href matching the existing dashboard pattern. Prefer textContent for free-form values; the Finviz link uses the same trusted pattern already shipped in index.html. No user-supplied input is rendered. |
| T-05-08 | Information disclosure | top.html exposes scores publicly | accept | Public exposure is the product goal (shareable URL). Market data only, no PII, no secrets. |
| T-05-09 | Tampering | New CDN dependency | accept | No new CDN added — top.html loads only style.css + app.js + (optionally) the already-shipping Tabulator. UI-SPEC Registry Safety confirms zero new third-party deps. |
| T-05-SC | Tampering | npm/pip installs | accept | Zero new packages; no build step. No supply-chain surface. |

No secrets in any frontend file. The coverage flag is JSON-only this phase (surfaced in Phase 7 stats.html per UI-SPEC).
</threat_model>

<verification>
- node -e checks above pass (app.js primitives, top.html structure, index.html columns + sort).
- All three pages load app.js and render nav via buildNav() (no hardcoded nav remains except the empty container).
- top.html fetch is cache-busted with ?v=Date.now().
- No new CDN <script>/<link> beyond style.css + app.js + existing Tabulator.
- Human verification checkpoint approved.
</verification>

<success_criteria>
- PAGE-03: docs/app.js holds the shared fetch/format/color/freshness primitives, reused across pages (no build step).
- PAGE-01: docs/top.html shows Top 10/25 (toggle) ranked by OverallScore with pillar sub-scores and headline signals.
- PAGE-04: site nav links Dashboard, Top Picks, Methodology across all pages (Stats deferred to Phase 7, array-driven for trivial addition).
- TRAP-02: a value-trap badge is displayed on each trapped Top-N card.
- SCORE-05/SCORE-08 (UI surfacing): pillar sub-scores appear as dashboard columns and the dashboard sorts by OverallScore.
</success_criteria>

<output>
Create .planning/phases/05-score-foundation-public-top-n/05-03-SUMMARY.md when done. Record: the app.js export surface, the top.html card structure, any UI-SPEC deviations and why, and the human-verify outcome.
</output>
