---
phase: 06-cheap-factors-sector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stock_screener.py
  - diagnose_finnhub.py
  - tests/test_factors_phase6.py
autonomous: true
requirements: [SECTOR-01, SIGNAL-01, SIGNAL-02, SIGNAL-03, SIGNAL-04, SIGNAL-05, SIGNAL-06, SIGNAL-07]

must_haves:
  truths:
    - "Every row dict from get_combined_data() carries a Sector string (or None) sourced from yfinance .info['sector'] (SECTOR-01, D-02)"
    - "Every row dict carries dist_52w_high, dist_52w_low, dist_5y_low, weeks_since_52w_low, weeks_since_5y_low (or None) from ONE reused 5y weekly history fetch (SIGNAL-01/02/03, D-03)"
    - "Every row dict carries fcf_yield, ev_ebit, earnings_yield, roic, shareholder_yield (or None) with per-field coverage handling (SIGNAL-04/05/06/07, D-01)"
    - "FCF is sourced from yfinance cashflow (OCF + capex, capex negative), NOT the dead Finnhub field (D-01)"
    - "shareholder_yield carries a shareholder_yield_partial flag when only dividend yield is available (SIGNAL-07)"
    - "Pure factor/distance helpers compute correctly on sample inputs via offline vanilla-assert tests with dummy env keys"
    - "diagnose_finnhub.py prints presence/absence for all [VERIFY] Finnhub field names for the next live Actions run"
  artifacts:
    - path: "stock_screener.py"
      provides: "Sector + 5y-weekly distance/recency signals + 4 fundamental factors as new row-dict fields; pure helpers _yf_row, _compute_price_signals, _compute_fcf_yield, _compute_ev_ebit, _compute_roic, _compute_shareholder_yield"
    - path: "tests/test_factors_phase6.py"
      provides: "Offline vanilla-assert tests for the new pure helpers"
      contains: "os.environ.setdefault"
    - path: "diagnose_finnhub.py"
      provides: "[VERIFY] Finnhub field names added to the inspection list"
  key_links:
    - from: "stock_screener.py get_combined_data()"
      to: "get_yf_price_and_history() / get_yf_fundamentals()"
      via: "single reused yf.Ticker object per ticker"
      pattern: "t\\.(history|info|cashflow|income_stmt|balance_sheet)"
    - from: "tests/test_factors_phase6.py"
      to: "stock_screener pure helpers"
      via: "import after os.environ.setdefault dummy keys"
      pattern: "from stock_screener import"
---

<objective>
Produce the new per-row DATA fields for Phase 6 — the GICS Sector string, the 5-year-weekly price distance/recency signals, and the four cheap-factor fundamentals (FCF yield, EV/EBIT + earnings yield, ROIC, shareholder yield) — emitted additively on the row dict from get_combined_data()/process_ticker(), each with per-field coverage handling.

Purpose: These raw fields are the inputs Plan 06-02 will fold into the Phase 5 4-pillar composite. This plan ONLY produces the data layer (sourcing + fallbacks + coverage flags). It does NOT touch scoring, thresholds, bands, or overall_score() — that is Plan 06-02. (D-01, D-02, D-03, D-04 raw-field-production scope only.)

Output: New row-dict fields (Sector, Dist_52w_High_Pct, Dist_52w_Low_Pct, Dist_5y_Low_Pct, Weeks_Since_52w_Low, Weeks_Since_5y_Low, FCF_Yield_Pct, EV_EBIT, Earnings_Yield_Pct, ROIC_Pct, Shareholder_Yield_Pct + a shareholder_yield_partial / short_history coverage flag) plus pure helpers, offline tests, and the diagnose_finnhub.py [VERIFY] field list.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cheap-factors-sector/06-CONTEXT.md
@.planning/phases/06-cheap-factors-sector/06-RESEARCH.md
@CLAUDE.md

# Source files to extend (read the cited line ranges before editing)
@stock_screener.py
@diagnose_finnhub.py
@tests/test_growth_trap_fixes.py
</context>

<artifacts_this_phase_produces>
The exact NEW row-dict keys Plan 06-02 will consume (additive only — no existing key is renamed or removed). Use these literal key names so 06-02 can read them directly:

| Row-dict key | Type | Source / formula | Coverage handling |
|---|---|---|---|
| `Sector` | str \| None | yfinance `.info['sector']` (D-02) | None on .info failure or absent key |
| `Dist_52w_High_Pct` | float \| None | (high_52w − price)/high_52w * 100, clamped ≥ 0 | None if history thin/empty or high_52w==0 |
| `Dist_52w_Low_Pct` | float \| None | (price − low_52w)/low_52w * 100, clamped ≥ 0 | None if history thin/empty or low_52w==0 |
| `Dist_5y_Low_Pct` | float \| None | (price − low_5y)/low_5y * 100, clamped ≥ 0 | None if history thin/empty or low_5y==0 |
| `Weeks_Since_52w_Low` | float \| None | len(w52)-1 − argmin(w52) | None if history thin/empty |
| `Weeks_Since_5y_Low` | float \| None | len(closes)-1 − argmin(closes) | None if history thin/empty |
| `FCF_Yield_Pct` | float \| None | FCF / market_cap * 100; FCF = OCF + capex (capex negative) (D-01) | None if FCF or market_cap None; may be negative |
| `EV_EBIT` | float \| None | (market_cap + total_debt − cash) / EBIT | None if EBIT None or EBIT ≤ 0 or EV ≤ 0 |
| `Earnings_Yield_Pct` | float \| None | EBIT / EV * 100 (inverse of EV/EBIT) | None if EBIT None or EBIT ≤ 0 or EV ≤ 0 |
| `ROIC_Pct` | float \| None | EBIT*(1−0.21) / (total_debt + equity − cash) * 100 | None if components insufficient or invested ≤ 0; may be negative |
| `Shareholder_Yield_Pct` | float \| None | (div_yield + net_buyback_yield) * 100 | div-only when buyback None |
| `shareholder_yield_partial` | bool | True when net_buyback_yield is None (div-only) | always present |
| `short_history` | bool | True when 8 ≤ weekly bars < 52 (recent IPO) | always present (False when ≥ 52) |

NEW pure helpers (package-private, internal — for tests only per CLAUDE.md §B):
- `_yf_row(df, labels) -> float | None` — most-recent annual value for first matching label (newest column first)
- `_compute_price_signals(closes, price) -> dict` — the five distance/recency signals + short_history from a Close series and current price
- `_compute_fcf_yield(ocf, capex, market_cap) -> float | None`
- `_compute_ev_ebit(ebit, total_debt, cash, market_cap) -> (ev_ebit, earnings_yield)` tuple
- `_compute_roic(ebit, total_debt, equity, cash) -> float | None`
- `_compute_shareholder_yield(div_yield, shares_now, shares_prev) -> (yield, partial_flag)` tuple

NOTE on percent units: all *_Pct fields emit whole-number percents (e.g. 5.0 = 5%) to match the existing `DivYield_Pct` / `Growth_g_Pct` convention. `EV_EBIT` is a raw multiple (not a percent). The 06-02 scoring bands in 06-RESEARCH.md Slice C are written against these percent-unit fields — keep them consistent.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure helpers + offline tests for distance/recency + 4 fundamental factor math</name>
  <read_first>
    - stock_screener.py:582-588 (_safe_float — reuse, do NOT reimplement)
    - stock_screener.py:604-618 (existing yfinance label-scan pattern to mirror for _yf_row)
    - tests/test_growth_trap_fixes.py:1-36 (offline vanilla-assert + os.environ.setdefault BEFORE import pattern to mirror exactly)
    - 06-RESEARCH.md Slice A §1b/2b/3b/4b (FCF/EV-EBIT/ROIC/shareholder-yield formulas + candidate label lists)
    - 06-RESEARCH.md Slice B "Distance and Recency Computation" + "Edge Cases" (exact pandas math, min-bar guard, zero-denominator guard)
  </read_first>
  <action>
    In stock_screener.py, add the NEW pure helpers near the other pure helpers (after _safe_float / get_combined_data region) so they are importable and unit-testable with NO network:

    (a) `_yf_row(df, labels)` — iterate the candidate `labels`, return `_safe_float(df.loc[label, df.columns[0]])` for the first label present in `df.index` (newest column is index 0), else None. Guard `df is None or df.empty`.

    (b) `_compute_price_signals(closes, price)` — takes a pandas Series `closes` (oldest→newest is NOT assumed; use the research's iloc slicing: w52 = closes.iloc[-min(n,52):]) and current `price`. Return a dict with keys dist_52w_high, dist_52w_low, dist_5y_low, weeks_since_52w_low, weeks_since_5y_low, short_history. Apply: empty/None or n<8 → all five signals None (short_history False); 8<=n<52 → compute over available window, short_history True; n>=52 → short_history False. Clamp each distance to >= 0.0 via max(0.0, ...). Zero-denominator guard: if high_52w/low_52w/low_5y == 0, set the affected signal to None. weeks_since = len(window)-1 - int(window.values.argmin()). Emit distances as whole-number percents (×100 already in the formula).

    (c) `_compute_fcf_yield(ocf, capex, market_cap)` — FCF = ocf + capex when both present (capex is negative in yfinance → addition; per A2 caution); if capex None but ocf present, FCF = ocf (OCF proxy); if ocf None → return None. fcf_yield = FCF / market_cap * 100 when market_cap truthy, else None. Negative FCF yields a negative result (carried forward, NOT clamped — 06-02 routes it to worst).

    (d) `_compute_ev_ebit(ebit, total_debt, cash, market_cap)` — total_debt/cash default to 0 when None; ev = market_cap + total_debt − cash; return (ev_ebit, earnings_yield) where both are None unless (ebit is not None and ebit > 0 and ev > 0); ev_ebit = ev/ebit; earnings_yield = ebit/ev*100 (whole-number percent). market_cap None → (None, None).

    (e) `_compute_roic(ebit, total_debt, equity, cash)` — cash defaults 0 when None; require ebit/total_debt/equity all not None; invested = total_debt + equity − cash; if invested <= 0 → None (data anomaly per Slice A §3d, NOT worst); roic = ebit*(1−0.21)/invested*100 (whole-number percent). Negative EBIT yields negative ROIC (carried forward, NOT clamped).

    (f) `_compute_shareholder_yield(div_yield, shares_now, shares_prev)` — div_yield already a whole-number percent (e.g. 1.5). net_buyback = (shares_prev − shares_now)/shares_prev*100 when shares_now and shares_prev and shares_prev>0, else None. Return (div_yield + (net_buyback or 0.0), partial_flag) where partial_flag = (net_buyback is None).

    Mark every helper's docstring "internal — for tests only" per CLAUDE.md §B (no new public surface). Use individual imports; no wildcards; no fully-qualified names (CLAUDE.md §A).

    Then create tests/test_factors_phase6.py mirroring tests/test_growth_trap_fixes.py EXACTLY: os.environ.setdefault("FRED_API_KEY","test") and ("FINNHUB_API_KEY","test") BEFORE `from stock_screener import ...`; _REPO_ROOT sys.path insert; vanilla `def test_*()` functions with bare `assert`; a `run_all()` that prints PASS/FAIL and sys.exit(1) on failure; `if __name__ == "__main__": run_all()`. Cover, with hand-computed expected values: FCF positive (capex negative addition, e.g. ocf=100, capex=-30, mktcap=1000 → 7.0), FCF negative, FCF None when ocf None; EV/EBIT + earnings-yield happy path and ebit<=0 → (None,None) and ev<=0 → (None,None); ROIC happy path, invested<=0 → None, negative-EBIT → negative; shareholder yield div-only (partial True) and div+buyback (partial False) and dilution (negative buyback); price signals via a small synthetic pandas Series for n>=52 (full), 8<=n<52 (short_history True), n<8 (all None), and the at-the-low case (dist_52w_low == 0.0, weeks_since_52w_low == 0). Build the Series with pandas (already a dependency) — NO network, NO yf.Ticker.
  </action>
  <verify>
    <automated>cd "C:\Python Projects\graham_screener\.claude\worktrees\nifty-goldberg-0c9a42"; python -c "import ast; ast.parse(open('stock_screener.py').read()); ast.parse(open('tests/test_factors_phase6.py').read()); print('OK')"; python tests/test_factors_phase6.py</automated>
  </verify>
  <done>tests/test_factors_phase6.py runs offline with dummy env keys and all assertions pass; the six new pure helpers exist and are imported by the test; no network call occurs (no yf.Ticker / requests in the test).</done>
</task>

<task type="auto">
  <name>Task 2: Wire Sector + 5y-weekly history + factor fetches into the reused Ticker, emit fields on the row dict</name>
  <read_first>
    - stock_screener.py:591-622 (get_yf_price_and_history — the single yf.Ticker(ticker) at line 598 is the object to reuse for .info, .history, .cashflow, .income_stmt, .balance_sheet)
    - stock_screener.py:625-698 (get_combined_data — where the new fields join the unified dict; market_cap_b * 1e9 is the FCF/EV denominator)
    - stock_screener.py:996-1044 (process_ticker — where fund[...] fields become row["..."] columns; mirror the existing round()/None pattern, e.g. row["DivYield_Pct"])
    - 06-RESEARCH.md Slice B "Sector" + "5-Year Weekly History Fetch" (the t.info.get + t.history(period="5y", interval="1wk") calls, fail-safe block)
    - 06-RESEARCH.md Slice A §5 "Statement Fetch Architecture" (each of t.cashflow / t.income_stmt / t.balance_sheet accessed at most ONCE per ticker; reuse ebit/total_debt/cash across EV-EBIT and ROIC)
  </read_first>
  <action>
    Extend get_yf_price_and_history() to reuse the SAME `t = yf.Ticker(ticker)` object (D-03 / D-05 discretion — one Ticker, no second construction) to additionally fetch, all inside the existing try/except so any failure fails safe to None:
    - sector via `t.info.get("sector")` (None-safe; wrap so an .info exception leaves sector None without aborting the price read — note .info may raise, so guard it specifically).
    - 5y weekly history via `t.history(period="5y", interval="1wk")`; pass its `["Close"]` series and the already-read fast_info price into `_compute_price_signals(...)` from Task 1; merge the returned dict (the five signals + short_history) into result.
    - raw statement rows for the four factors: ocf+capex from `t.cashflow` (via `_yf_row` with the OCF_LABELS/CAPEX_LABELS lists from 06-RESEARCH Slice A §1b), ebit from `t.income_stmt` (EBIT_LABELS), total_debt = `_yf_row(bs, TOTAL_DEBT_LABELS)` + `_yf_row(bs, CURRENT_DEBT_LABELS)` (each `or 0`), cash (CASH_LABELS), equity (EQUITY_LABELS), and shares_now/shares_prev from the SHARES_LABELS row (iloc[0]/iloc[1]) of `t.balance_sheet`. Access each statement attribute exactly once; store the candidate-label lists as module-level constants near the helpers. Return these raw components in the result dict (e.g. result["ocf"], result["capex"], result["ebit"], result["total_debt"], result["cash"], result["equity"], result["shares_now"], result["shares_prev"], result["sector"], plus the five signal keys + short_history).

    In get_combined_data(): compute the factor values from the raw components using the Task 1 pure helpers — fcf_yield = _compute_fcf_yield(ocf, capex, market_cap=mkt_cap_b*1e9); (ev_ebit, earnings_yield) = _compute_ev_ebit(ebit, total_debt, cash, mkt_cap_b*1e9); roic = _compute_roic(ebit, total_debt, equity, cash); div_yield computed from existing ttm_dps/price the same way DivYield_Pct is (whole-number percent), then (shareholder_yield, sh_partial) = _compute_shareholder_yield(div_yield, shares_now, shares_prev). Add sector + the five price signals + short_history + the five factor values + sh_partial to the returned dict. Guard mkt_cap_b None → market_cap None → fcf_yield/ev_ebit None (D-01b).

    In process_ticker(): emit the new flat columns using the literal key names in <artifacts_this_phase_produces> (Sector, Dist_52w_High_Pct, Dist_52w_Low_Pct, Dist_5y_Low_Pct, Weeks_Since_52w_Low, Weeks_Since_5y_Low, FCF_Yield_Pct, EV_EBIT, Earnings_Yield_Pct, ROIC_Pct, Shareholder_Yield_Pct, shareholder_yield_partial, short_history). round() floats to 2dp where present, leave None as None (mirror row["MarketCap_B"] None-guard). Do NOT call overall_score with new args, do NOT add scoring/bands — that is 06-02. Additive only; touch no existing column.

    Do NOT add the dead Finnhub FCF field path — the existing fcf_per_share Finnhub read at lines 674-697 stays as-is (it still feeds the Plan-02 trap gate sign); FCF YIELD is the new yfinance-sourced field and is independent. Surgical changes only (CLAUDE.md §3); no new pip deps; no new JSON files.
  </action>
  <verify>
    <automated>cd "C:\Python Projects\graham_screener\.claude\worktrees\nifty-goldberg-0c9a42"; python -c "import ast; ast.parse(open('stock_screener.py').read()); print('parse OK')"; python -c "import os; os.environ.setdefault('FRED_API_KEY','test'); os.environ.setdefault('FINNHUB_API_KEY','test'); import stock_screener as s; import inspect; src=inspect.getsource(s.process_ticker); [print('MISSING',k) or exit(1) for k in ['Sector','Dist_52w_High_Pct','Dist_52w_Low_Pct','Dist_5y_Low_Pct','Weeks_Since_52w_Low','Weeks_Since_5y_Low','FCF_Yield_Pct','EV_EBIT','Earnings_Yield_Pct','ROIC_Pct','Shareholder_Yield_Pct'] if k not in src]; print('all 11 new columns referenced in process_ticker')"; python tests/test_factors_phase6.py</automated>
  </verify>
  <done>stock_screener.py imports cleanly with dummy keys; process_ticker references all 11 new flat-column key names; only ONE yf.Ticker(ticker) construction exists in get_yf_price_and_history; each of .cashflow/.income_stmt/.balance_sheet is accessed at most once; Task 1 tests still pass; no new pip dependency added.</done>
</task>

<task type="auto">
  <name>Task 3: Add the [VERIFY] Finnhub field names to diagnose_finnhub.py's inspection list</name>
  <read_first>
    - diagnose_finnhub.py:46-60 (the fields_of_interest list to extend)
    - 06-RESEARCH.md Slice A §6 "Finnhub Fields Requiring Live Validation" (the [VERIFY] table — the exact field names to add)
  </read_first>
  <action>
    In diagnose_finnhub.py, extend the `fields_of_interest` list (lines ~46-60) with the [VERIFY] Finnhub field names from 06-RESEARCH Slice A §6 so the next live Actions run prints their presence/absence per ticker: evToEbit, ebitAnnual, enterpriseValue, roiAnnual, dividendYieldAnnual, sharesBuybackRatioAnnual, totalDebtAnnual, cashAnnual (freeCashFlowPerShareTTM/Annual are already present from Phase 5). Add a brief inline comment "# Phase 6 [VERIFY] — confirm free-tier presence" above the new block. Do NOT change the request logic, the ticker list, or the statement-dump sections — additive list edit only (surgical, CLAUDE.md §3). This is the deferred live-coverage confirmation path: the executor has no API keys and cannot run this live, so the field list is staged for the next Actions run.
  </action>
  <verify>
    <automated>cd "C:\Python Projects\graham_screener\.claude\worktrees\nifty-goldberg-0c9a42"; python -c "import ast; ast.parse(open('diagnose_finnhub.py').read()); print('parse OK')"; python -c "src=open('diagnose_finnhub.py',encoding='utf-8').read(); miss=[f for f in ['evToEbit','ebitAnnual','enterpriseValue','roiAnnual','dividendYieldAnnual','sharesBuybackRatioAnnual','totalDebtAnnual','cashAnnual'] if f not in src]; print('MISSING',miss) if miss else print('all 8 VERIFY fields present')"</automated>
  </verify>
  <done>diagnose_finnhub.py parses; all 8 new [VERIFY] field names appear in the source; the ticker list and request logic are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yfinance .info / .history / statements → screener | Untrusted external market data crosses here (web-scraped, may be None, NaN, stale, or raise) |
| Finnhub metric=all → screener | Untrusted external JSON (free-tier silently omits fields) |
| screener row dict → results.json (public) | Market-data-only output, no secrets, no PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | yfinance None/NaN/missing-label inputs corrupting factor math | mitigate | All inputs routed through `_safe_float` + None guards in the pure helpers; zero-denominator and `invested<=0` guards return None rather than raising (Slice A/B edge cases) |
| T-06-02 | Denial of Service | `.info` web-scrape + extra `.history`/statement fetches add latency across ~550 tickers | accept | One reused yf.Ticker per ticker; fail-safe try/except returns None on timeout; real cache is Phase 7 (DATA-03), accepted added latency per CONTEXT runtime note |
| T-06-03 | Information Disclosure | new fields emitted to the public results.json | accept | Market-data-only (sector string + ratios); no secrets, no PII; min-row guard (<100) already protects against empty commits |
| T-06-04 | Tampering | unverified [VERIFY] Finnhub field names silently returning wrong-tier data (EBITDA-as-EBIT) | mitigate | Do NOT silently fall back to EV/EBITDA (Slice A §2a); yfinance component computation is the fallback; diagnose_finnhub.py stages live confirmation for the next Actions run |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies introduced (yfinance + pandas + requests already present); no install task in this plan, so no package-legitimacy checkpoint required |
</threat_model>

<verification>
- `python tests/test_factors_phase6.py` → all assertions pass (offline, dummy env keys).
- `python -c "import ast; ast.parse(open('stock_screener.py').read())"` and same for diagnose_finnhub.py and the new test file → all parse.
- `import stock_screener` succeeds with dummy FRED/FINNHUB keys (no live call at import).
- process_ticker source references all 11 new flat columns; only one yf.Ticker(ticker) construction exists.
- diagnose_finnhub.py contains all 8 [VERIFY] field names.
- Existing tests still pass: `python tests/test_scoring.py` and `python tests/test_growth_trap_fixes.py` (no regression — additive change only).
</verification>

<success_criteria>
- Sector + 5 distance/recency signals + 5 fundamental factors emitted as additive row-dict fields with the exact key names in <artifacts_this_phase_produces> (SECTOR-01, SIGNAL-01..07).
- FCF sourced from yfinance cashflow (OCF + negative capex), NOT the dead Finnhub field (D-01).
- shareholder_yield_partial + short_history coverage flags present (SIGNAL-07, D-01b).
- All factor/distance math lives in offline-testable pure helpers; tests pass with dummy keys.
- ONE yf.Ticker per ticker; each statement accessed at most once (D-03/D-05).
- No scoring/threshold/overall_score changes (that is 06-02); no new pip deps; no new JSON files; additive-only schema.
- diagnose_finnhub.py [VERIFY] fields staged for the next live Actions run (live coverage confirmation deferred — executor has no API keys).
</success_criteria>

<output>
Create `.planning/phases/06-cheap-factors-sector/06-01-SUMMARY.md` when done. In it, explicitly list the final emitted row-dict key names and the pure-helper names so Plan 06-02 can consume them without re-reading the source, and note that live Finnhub field-name/coverage confirmation is deferred to the next GitHub Actions run via diagnose_finnhub.py.
</output>
