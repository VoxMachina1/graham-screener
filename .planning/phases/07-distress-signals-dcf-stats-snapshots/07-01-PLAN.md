---
phase: 07-distress-signals-dcf-stats-snapshots
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - stock_screener.py
  - requirements.txt
  - tests/test_distress_phase7.py
  - tests/test_dcf_phase7.py
autonomous: true
requirements: [SIGNAL-08, SIGNAL-09, DCF-01, DCF-02, DCF-03]
must_haves:
  truths:
    - "_compute_piotroski() returns an int 0-9 for two-year fixture statements, None when no statements"
    - "_compute_altman_z() returns Z'' = 6.56·X1 + 3.26·X2 + 6.72·X3 + 1.05·X4 for fixture inputs, None when inputs absent"
    - "_compute_dcf_forward() returns (intrinsic_value, discount_pct) and raises ValueError when terminal_growth >= WACC"
    - "_compute_dcf_reverse() returns (implied_growth, True) for a solvable fixture and (None, False) on no-root bracket"
    - "get_yf_price_and_history() returns income_stmt_df, balance_sheet_df, cashflow_df raw DataFrame keys"
    - "scipy is present in requirements.txt so the Actions runner can import scipy.optimize.brentq"
  artifacts:
    - path: "stock_screener.py"
      provides: "_compute_piotroski, _compute_altman_z, _compute_dcf_forward, _compute_dcf_reverse, _yf_row_prev, _dcf_wacc pure helpers + new label lists + DCF/Piotroski/Altman SCORE_* config + raw DataFrame threading"
      contains: "def _compute_piotroski"
    - path: "tests/test_distress_phase7.py"
      provides: "Piotroski + Altman + _yf_row_prev offline unit tests"
      contains: "_compute_piotroski"
    - path: "tests/test_dcf_phase7.py"
      provides: "Forward DCF + reverse DCF solver offline unit tests"
      contains: "_compute_dcf_reverse"
    - path: "requirements.txt"
      provides: "scipy pinned for the Actions runner"
      contains: "scipy"
  key_links:
    - from: "get_yf_price_and_history()"
      to: "income_stmt_df / balance_sheet_df / cashflow_df result keys"
      via: "raw DataFrame stored in return dict"
      pattern: "income_stmt_df"
    - from: "_compute_dcf_reverse()"
      to: "scipy.optimize.brentq"
      via: "bounded root-find with sign-change bracket guard"
      pattern: "brentq"
---

<objective>
Build the Phase 7 data layer: the pure numeric helpers that compute Piotroski F-Score, Altman Z'', and forward + reverse two-stage DCF, plus the yfinance plumbing that feeds them (two-year statement reads via raw DataFrame threading and new candidate-label lists), plus their offline test suites and the scipy dependency the Actions runner needs.

Purpose: These helpers are consumed by the Wave 2 scoring fold (overall_score / process_ticker). Defining them first — with their contracts locked by tests — lets Wave 2 wire them in without exploring the codebase. No scoring or JSON changes happen here; this plan is additive and leaves overall_score() untouched. Implements SIGNAL-08 (Piotroski), SIGNAL-09 (Altman Z''), DCF-01 (forward DCF), DCF-02 (reverse DCF), DCF-03 (terminal<WACC assert + bounded solver → None per D-03/D-06/D-07/D-08/D-09).

Output: New pure helpers (`_compute_piotroski`, `_compute_altman_z`, `_compute_dcf_forward`, `_compute_dcf_reverse`, `_yf_row_prev`, `_dcf_wacc`), new SCORE_*/DCF_* config constants, new label lists, three new raw-DataFrame keys on `get_yf_price_and_history()`, scipy in requirements.txt, and two new test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-distress-signals-dcf-stats-snapshots/07-CONTEXT.md
@.planning/phases/07-distress-signals-dcf-stats-snapshots/07-RESEARCH.md
@.planning/phases/07-distress-signals-dcf-stats-snapshots/07-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols/files created by THIS plan (Wave 1):

**New pure helpers in `stock_screener.py`:**
- `_yf_row_prev(df, labels) -> float | None` — prior-year (column index 1) reader, guards `df.shape[1] < 2`
- `_dcf_wacc(aaa_yield_pct: float) -> float` — returns `(aaa_yield_pct + DCF_ERP) / 100.0`
- `_compute_piotroski(inc_curr, inc_prev, bs_curr, bs_prev, cf_curr, cf_prev) -> int | None`
- `_compute_altman_z(bs_curr, inc_curr) -> float | None`
- `_compute_dcf_forward(eps, g_cagr_pct, aaa_yield_pct, price) -> tuple` → `(intrinsic_value, discount_pct)` or `(None, None)`
- `_compute_dcf_reverse(price, eps, aaa_yield_pct, g_stage1_pct) -> tuple` → `(implied_growth_pct | None, converged: bool)`

**New config constants in `stock_screener.py` SCORE_* block:**
- `SCORE_PIOTROSKI_BANDS` (5 bands, ascending, `[ASSUMED]`)
- `SCORE_ALTMAN_DISTRESS = 1.1`, `SCORE_ALTMAN_SAFE = 2.6`, `SCORE_ALTMAN_BANDS` (3 bands, `[ASSUMED]`)
- `SCORE_DCF_DISCOUNT_WIN_LO`, `SCORE_DCF_DISCOUNT_WIN_HI`, `SCORE_DCF_DISCOUNT_BANDS` (ascending, `[ASSUMED]` — consumed in Wave 2)
- `DCF_ERP = 5.5`, `DCF_TERMINAL_GROWTH_CAP = 3.0`, `DCF_EXCLUDED_SECTORS = {"Financial Services", "Real Estate"}`, `ALTMAN_EXCLUDED_SECTORS = {"Financial Services"}` (sector sets consumed in Wave 2)

**New label lists in `stock_screener.py`** (placed beside existing EBIT_LABELS etc., lines 750-803):
- `NET_INCOME_LABELS`, `TOTAL_ASSETS_LABELS`, `GROSS_PROFIT_LABELS`, `REVENUE_LABELS`, `CURRENT_ASSETS_LABELS`, `CURRENT_LIABILITIES_LABELS`, `LONG_TERM_DEBT_LABELS`, `RETAINED_EARNINGS_LABELS`, `TOTAL_LIABILITIES_LABELS`

**New keys on `get_yf_price_and_history()` return dict:**
- `income_stmt_df`, `balance_sheet_df`, `cashflow_df` (raw DataFrame | None)

**New files:**
- `tests/test_distress_phase7.py`, `tests/test_dcf_phase7.py`

**requirements.txt addition:** `scipy>=1.10`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scipy dependency, new label lists, DCF config constants, and the two-year + WACC helper primitives</name>
  <files>requirements.txt, stock_screener.py, tests/test_distress_phase7.py</files>
  <read_first>
    - requirements.txt (current 6 lines — scipy is NOT listed; it must be added so the Actions runner can import it)
    - stock_screener.py lines 750-816 (existing label-list block: OCF_LABELS, CAPEX_LABELS, EBIT_LABELS, TOTAL_DEBT_LABELS, CURRENT_DEBT_LABELS, CASH_LABELS, EQUITY_LABELS, SHARES_LABELS; then _safe_float at 739 and _yf_row at 805)
    - stock_screener.py lines 82-296 (the SCORE_* config region where new constants belong, ending just before _piecewise_score at 297)
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-PATTERNS.md (sections "_yf_row_prev()" and "Shared Patterns / _safe_float()" and "SCORE_* config block pattern")
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-RESEARCH.md (Piotroski label-list block lines 238-274; Altman label-list block lines 328-338; Package Legitimacy Audit lines 123-133)
  </read_first>
  <behavior>
    - _yf_row_prev(df, labels): returns _safe_float of df.loc[label, df.columns[1]] for the first matching label; returns None when df is None, df.empty, or df.shape[1] < 2
    - _dcf_wacc(5.0) returns (5.0 + 5.5) / 100.0 = 0.105
    - NET_INCOME_LABELS through TOTAL_LIABILITIES_LABELS each exist as module-level lists of candidate strings
    - scipy importable: `python -c "from scipy.optimize import brentq"` exits 0
  </behavior>
  <action>
    Add `scipy>=1.10` to requirements.txt (scipy is Approved in the RESEARCH.md Package Legitimacy Audit — already installed locally at 1.15.3 but ABSENT from the manifest; the Actions runner does a clean `pip install -r requirements.txt` and would fail to import brentq without this line).

    Add nine new module-level candidate-label lists immediately after the existing SHARES_LABELS block (~line 803), matching the exact list style of OCF_LABELS/EBIT_LABELS: NET_INCOME_LABELS, TOTAL_ASSETS_LABELS, GROSS_PROFIT_LABELS, REVENUE_LABELS, CURRENT_ASSETS_LABELS, CURRENT_LIABILITIES_LABELS, LONG_TERM_DEBT_LABELS, RETAINED_EARNINGS_LABELS, TOTAL_LIABILITIES_LABELS. Use the exact candidate strings enumerated in RESEARCH.md lines 238-274 and 328-338. Add an `# [ASSUMED — yfinance label names vary by ticker; validated on a live Actions run]` comment above the block.

    Add the DCF + Altman/Piotroski config constants into the SCORE_* config region (after the existing Phase-6 SCORE_* constants, before line 297 _piecewise_score): DCF_ERP = 5.5, DCF_TERMINAL_GROWTH_CAP = 3.0, DCF_EXCLUDED_SECTORS = {"Financial Services", "Real Estate"}, ALTMAN_EXCLUDED_SECTORS = {"Financial Services"}, SCORE_PIOTROSKI_BANDS (5 ascending bands per PATTERNS.md), SCORE_ALTMAN_DISTRESS = 1.1, SCORE_ALTMAN_SAFE = 2.6, SCORE_ALTMAN_BANDS (3 bands per PATTERNS.md), and SCORE_DCF_DISCOUNT_WIN_LO / SCORE_DCF_DISCOUNT_WIN_HI / SCORE_DCF_DISCOUNT_BANDS (ascending — used in Wave 2 for the DCF Value sub-group). Tag every band/threshold with `# [ASSUMED]`. Defining DCF_ERP here means _dcf_wacc can reference it at module load.

    Add `_yf_row_prev(df, labels) -> float | None` immediately after `_yf_row()` (line 816), copying _yf_row's structure but reading `df.columns[1]` and guarding `df.shape[1] < 2` (return None). Include the `internal — for tests only.` docstring line.

    Add `_dcf_wacc(aaa_yield_pct: float) -> float` returning `(aaa_yield_pct + DCF_ERP) / 100.0`. Place it after _yf_row_prev.

    Create tests/test_distress_phase7.py with the standard offline header (os.environ.setdefault FRED_API_KEY/FINNHUB_API_KEY BEFORE importing stock_screener; _REPO_ROOT sys.path insert — copy verbatim from tests/test_factors_phase6.py). Import _yf_row_prev and _dcf_wacc. Add tests: _yf_row_prev returns prior-year value from a 2-column fixture DataFrame; returns None for a 1-column DataFrame; returns None for None/empty; _dcf_wacc(5.0) == 0.105. Add a run_all() + `if __name__ == "__main__"` PASS/FAIL block copied from test_factors_phase6.py. (Piotroski/Altman/DCF tests are added in Tasks 2-3.)
  </action>
  <verify>
    <automated>cd "C:/Python Projects/graham_screener/.claude/worktrees/nifty-goldberg-0c9a42" && python -c "from scipy.optimize import brentq; print('scipy ok')" && python tests/test_distress_phase7.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^scipy' requirements.txt` returns 1
    - `python -c "from scipy.optimize import brentq"` exits 0
    - `grep -E 'NET_INCOME_LABELS|RETAINED_EARNINGS_LABELS|TOTAL_LIABILITIES_LABELS' stock_screener.py` matches all three list names
    - `grep -E 'DCF_ERP|DCF_TERMINAL_GROWTH_CAP|DCF_EXCLUDED_SECTORS|ALTMAN_EXCLUDED_SECTORS|SCORE_PIOTROSKI_BANDS|SCORE_ALTMAN_BANDS|SCORE_DCF_DISCOUNT_BANDS' stock_screener.py` matches all seven names
    - `def _yf_row_prev` and `def _dcf_wacc` both present in stock_screener.py
    - `python tests/test_distress_phase7.py` prints all PASS and exits 0
  </acceptance_criteria>
  <done>scipy is in requirements.txt and importable; nine label lists, the DCF/Altman/Piotroski config constants, and the _yf_row_prev + _dcf_wacc helpers exist; the new test file passes its initial primitive tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _compute_piotroski + _compute_altman_z pure helpers and thread raw statement DataFrames through get_yf_price_and_history()</name>
  <files>stock_screener.py, tests/test_distress_phase7.py</files>
  <read_first>
    - stock_screener.py lines 876-925 (the _compute_fcf_yield / _compute_roic helper pattern to copy — None-guard, float|None return, "internal — for tests only." docstring)
    - stock_screener.py lines 945-1050 (get_yf_price_and_history — the result-dict init at 959-979, the statement reads at 995-1046, the outer try/except at 1048-1050)
    - stock_screener.py lines 297-331 (_piecewise_score, _winsorize, _avg_present — NOT used inside these compute helpers; referenced so the executor knows scoring stays in Wave 2)
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-RESEARCH.md (Piotroski 9-criterion table lines 207-301; Altman X1-X4 formula lines 305-360; Pitfall 1 statement column ordering lines 697-705; Pitfall 2 negative Z lines 707-711)
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-PATTERNS.md ("_compute_piotroski() and _compute_altman_z()" + "get_yf_price_and_history() additions" sections)
  </read_first>
  <behavior>
    - _compute_piotroski with two complete fixture years where every criterion passes → returns 9; where every criterion fails → returns 0
    - _compute_piotroski returns None when inc_curr/bs_curr/cf_curr are all None (no statements)
    - Two-year comparison criteria (F3, F5, F6, F8, F9) are skipped (not failed) when the prior-year statement is absent; single-year criteria still contribute
    - _compute_altman_z returns 6.56·X1 + 3.26·X2 + 6.72·X3 + 1.05·X4 for a fixture balance sheet + income statement
    - _compute_altman_z returns None when any of WC/TA, RE/TA, EBIT/TA, BVE/TL cannot be computed (missing inputs or total_assets/total_liabilities == 0)
    - _compute_altman_z produces a negative Z'' for a negative-equity fixture (does not crash)
    - get_yf_price_and_history() return dict contains keys income_stmt_df, balance_sheet_df, cashflow_df
  </behavior>
  <action>
    Implement `_compute_piotroski(inc_curr, inc_prev, bs_curr, bs_prev, cf_curr, cf_prev) -> int | None` as a pure helper next to _compute_roic (~line 925). Use a local `_get(df, labels)` that reads `df.columns[0]` via the label lists (do NOT sort the DataFrame — per Pitfall 1, columns[0] is newest, columns[1] prior). Sum the nine binary signals from the RESEARCH.md table: F1 ROA>0 (NET_INCOME / TOTAL_ASSETS, year 0), F2 OCF>0 (OCF_LABELS, year 0), F3 ROA improved (year 0 vs year 1), F4 OCF/TotalAssets > ROA (accruals), F5 long-term-debt/avg-assets decreased, F6 current ratio improved (CURRENT_ASSETS/CURRENT_LIABILITIES year 0 vs 1), F7 no dilution (shares year 0 <= year 1 — SHARES_LABELS), F8 gross margin improved (GROSS_PROFIT/REVENUE year 0 vs 1), F9 asset turnover improved (REVENUE/TOTAL_ASSETS year 0 vs 1). Apply the absent-data strategy from RESEARCH.md lines 295-299: a missing single-year input fails that criterion (contributes 0); a missing prior-year input for a comparison criterion skips it (do not count it); if ALL statements absent, return None. Docstring includes `internal — for tests only.`

    Implement `_compute_altman_z(bs_curr, inc_curr) -> float | None` as a pure helper. Compute X1 = (current_assets - current_liabilities) / total_assets; X2 = retained_earnings / total_assets; X3 = ebit / total_assets; X4 = equity / total_liabilities. Use the new label lists + existing EBIT_LABELS/EQUITY_LABELS. Return None if total_assets or total_liabilities is None/0 or any numerator input is None. Return 6.56*X1 + 3.26*X2 + 6.72*X3 + 1.05*X4. No winsorize/scoring here (that is Wave 2). Docstring includes `internal — for tests only.`

    Extend get_yf_price_and_history(): add `"income_stmt_df": None, "balance_sheet_df": None, "cashflow_df": None` to the result-dict init (after line 978). Inside the try block, after the existing statement reads, store the raw unsorted DataFrames: `result["income_stmt_df"] = t.income_stmt`, `result["balance_sheet_df"] = t.balance_sheet`, `result["cashflow_df"] = t.cashflow`. Store them raw (newest-first columns), NOT the `inc = inc.sort_index(axis=1)` sorted local used for EPS. Do not alter any existing extraction.

    Add tests to tests/test_distress_phase7.py: build synthetic income_stmt / balance_sheet / cashflow DataFrames (two columns each, newest-first, index = label strings) via small factory helpers. Cover: Piotroski all-pass→9, all-fail→0, no-statements→None, prior-year-missing skips comparison criteria; Altman known-fixture exact value (assert abs(z - expected) < 1e-6 with a hand-computed expected), Altman missing-input→None, Altman negative-equity→negative-z. Update run_all() to register the new tests.
  </action>
  <verify>
    <automated>cd "C:/Python Projects/graham_screener/.claude/worktrees/nifty-goldberg-0c9a42" && python tests/test_distress_phase7.py</automated>
  </verify>
  <acceptance_criteria>
    - `def _compute_piotroski` and `def _compute_altman_z` both present in stock_screener.py
    - Both helper docstrings contain the string `internal — for tests only.`
    - `grep -E 'income_stmt_df|balance_sheet_df|cashflow_df' stock_screener.py` matches at least 6 lines (3 in init, 3 in assignment)
    - `python tests/test_distress_phase7.py` prints all PASS and exits 0, including a test asserting Piotroski all-pass fixture == 9 and Altman fixture matches a hand-computed Z'' within 1e-6
    - No call to `_piecewise_score` or `_winsorize` appears inside `_compute_piotroski` or `_compute_altman_z` (scoring stays in Wave 2)
  </acceptance_criteria>
  <done>Piotroski and Altman pure helpers compute correct values from fixture DataFrames and return None on absent data; get_yf_price_and_history() now hands back the three raw statement DataFrames; all distress tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement forward + reverse two-stage DCF helpers with WACC assert and bounded scipy solver</name>
  <files>stock_screener.py, tests/test_dcf_phase7.py</files>
  <read_first>
    - stock_screener.py lines 876-925 (_compute_* helper conventions)
    - stock_screener.py SCORE_* config region where Task 1 added DCF_ERP / DCF_TERMINAL_GROWTH_CAP (verify those constants exist before writing the DCF math) and the _dcf_wacc helper Task 1 added
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-RESEARCH.md (Forward DCF lines 433-474; Reverse DCF lines 476-530 including the brentq bracket-guard reference implementation; Pitfall 3 terminal>=WACC lines 713-717; Pitfall 4 no-root edge lines 719-723)
    - tests/test_factors_phase6.py (run_all + __main__ PASS/FAIL block to copy for the new test file)
  </read_first>
  <behavior>
    - _compute_dcf_forward(eps=5.0, g_cagr_pct=8.0, aaa_yield_pct=5.0, price=80.0) returns a tuple (intrinsic_value, discount_pct) where discount_pct = (1 - price/intrinsic) * 100
    - Forward DCF terminal growth = min(g_cagr, DCF_TERMINAL_GROWTH_CAP/100); Stage 1 sums 5 discounted EPS years; terminal via Gordon Growth discounted to present
    - Misconfiguration where terminal_growth >= WACC raises ValueError with a config-diagnostic message naming DCF_ERP and DCF_TERMINAL_GROWTH_CAP
    - _compute_dcf_forward returns (None, None) when eps is None or eps <= 0 (no earnings to discount)
    - _compute_dcf_reverse returns (implied_growth_pct, True) for a price that has a root in [-50, 100]; round-trip: feeding the implied growth back into the forward valuation reproduces the price within tolerance
    - _compute_dcf_reverse returns (None, False) when _dcf_value(lo) and _dcf_value(hi) have the same sign (no root in bracket) — never a silent default (D-09)
  </behavior>
  <action>
    Implement `_compute_dcf_forward(eps, g_cagr_pct, aaa_yield_pct, price) -> tuple` returning `(intrinsic_value, discount_pct)` or `(None, None)`. Guard: eps None or <= 0 → (None, None). Compute wacc via _dcf_wacc(aaa_yield_pct); g = g_cagr_pct/100; g_terminal = min(g, DCF_TERMINAL_GROWTH_CAP/100). Assert g_terminal < wacc — if not, raise ValueError with the exact diagnostic message form from RESEARCH.md lines 466-470 (names both DCF_ERP and DCF_TERMINAL_GROWTH_CAP as the knobs to adjust). Stage 1: loop t=1..5 accumulating eps_t = eps*(1+g)^t discounted by (1+wacc)^t. Terminal: TV = eps_5*(1+g_terminal)/(wacc - g_terminal); PV(TV) = TV/(1+wacc)^5. intrinsic = PV(stage1)+PV(TV). discount_pct = (1 - price/intrinsic)*100 (positive = cheap). Docstring includes `internal — for tests only.`

    Implement `_compute_dcf_reverse(price, eps, aaa_yield_pct, g_stage1_pct) -> tuple` returning `(implied_growth_pct | None, converged: bool)`, following the RESEARCH.md reference implementation (lines 493-525) exactly: define inner `_dcf_value(g_pct)` mirroring the forward math with the same terminal-cap and a `g_term = wacc - 0.001` safety clamp; bounds lo=-50.0, hi=100.0; bracket guard `if _dcf_value(lo) * _dcf_value(hi) > 0: return (None, False)`; call `brentq(_dcf_value, lo, hi, xtol=1e-4, maxiter=100)`; wrap in try/except (ValueError, RuntimeError) → (None, False); return (round(root, 2), True) on success. Import brentq at the top of the module with the other imports (`from scipy.optimize import brentq`). Guard: eps None or <= 0 → (None, False).

    Create tests/test_dcf_phase7.py with the standard offline header (env vars before import; sys.path insert). Import _compute_dcf_forward, _compute_dcf_reverse, _dcf_wacc. Tests: forward returns finite intrinsic + correct discount sign for a cheap fixture and an expensive fixture; forward raises ValueError when called with a temporarily-overridden DCF_TERMINAL_GROWTH_CAP that exceeds WACC (or an aaa_yield low enough that the assert fires) — assert the exception is raised; forward (None, None) when eps None; reverse round-trip (compute forward intrinsic at known g, set price=intrinsic, assert reverse implied growth ≈ g within 0.1); reverse (None, False) for an extreme price with no sign change in the bracket. Add run_all() + __main__ PASS/FAIL block.
  </action>
  <verify>
    <automated>cd "C:/Python Projects/graham_screener/.claude/worktrees/nifty-goldberg-0c9a42" && python tests/test_dcf_phase7.py</automated>
  </verify>
  <acceptance_criteria>
    - `def _compute_dcf_forward` and `def _compute_dcf_reverse` both present in stock_screener.py
    - `from scipy.optimize import brentq` appears once in stock_screener.py imports
    - `python tests/test_dcf_phase7.py` prints all PASS and exits 0
    - One test asserts a ValueError is raised when terminal_growth >= WACC (try/except + a failure assert if no exception)
    - One test asserts _compute_dcf_reverse returns (None, False) for a no-root bracket and does NOT return a numeric default
    - One reverse round-trip test asserts implied growth ≈ input growth within 0.1 percentage points
  </acceptance_criteria>
  <done>Forward DCF returns intrinsic value + discount %, asserts terminal<WACC loudly; reverse DCF finds the implied growth via brentq and emits (None, False) on non-convergence; all DCF tests pass.</done>
</task>

</tasks>

<verification>
- `python tests/test_distress_phase7.py && python tests/test_dcf_phase7.py` both exit 0 with all PASS
- Full existing suite still green: `python tests/test_scoring.py && python tests/test_growth_trap_fixes.py && python tests/test_factors_phase6.py && python tests/test_scoring_phase6.py` (Wave 1 is additive — overall_score() unchanged, so no existing test should regress)
- `python -c "import stock_screener"` succeeds with FRED_API_KEY/FINNHUB_API_KEY set to test (no import-time crash from new constants/imports)
</verification>

<success_criteria>
- All six new pure helpers exist and are import-testable offline
- scipy is in requirements.txt and brentq imports
- Nine new label lists + DCF/sector config constants + Piotroski/Altman/DCF SCORE_* bands exist
- get_yf_price_and_history() returns the three raw statement DataFrames
- Two new test files pass; no existing test regresses
- overall_score(), process_ticker(), write_json() are UNCHANGED by this plan (Wave 2 owns those)
</success_criteria>

<output>
Create `.planning/phases/07-distress-signals-dcf-stats-snapshots/07-01-SUMMARY.md` when done — record the final signatures of all six helpers, the exact discount-sign convention, the final SCORE_PIOTROSKI_BANDS / SCORE_ALTMAN_BANDS / SCORE_DCF_DISCOUNT_BANDS values chosen, and confirm overall_score() was not touched.
</output>
