---
phase: 07-distress-signals-dcf-stats-snapshots
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - stock_screener.py
  - tests/test_scoring.py
  - tests/test_distress_phase7.py
autonomous: true
requirements: [TRAP-03, SECTOR-02, SIGNAL-08, SIGNAL-09, DCF-01, DCF-02, DCF-03, PAGE-02]
must_haves:
  truths:
    - "overall_score() Safety pillar is the average of piotroski_sub + altman_sub + defensive + de + cr — the is_trap parameter no longer exists in the signature"
    - "Piotroski-absent and Altman-absent contribute 50.0 to the Safety pillar (D-04), not an average-over-present skip"
    - "Financial Services tickers get altman_z=None and DCF=None; Real Estate tickers get DCF=None (SECTOR-02 / D-10 / D-11)"
    - "process_ticker() emits Piotroski_F, Altman_Z, DCF_Intrinsic_Value, DCF_Discount_Pct, DCF_Implied_Growth, dcf_reverse_converged flat columns"
    - "dcf_discount_pct is scored as a 4th Value sub-group; coverage leaf count is 17"
    - "write_json() emits piotroski/altman/dcf_discount sub-scores in the nested scores object and writes docs/data/stats.json"
    - "is_trap remains a diagnostic flat column and scores.trap key for backward compat, but no longer floors Safety to 0"
  artifacts:
    - path: "stock_screener.py"
      provides: "Safety-pillar rewrite, sector gate, process_ticker DCF/distress wiring, _compute_stats(), stats.json writer"
      contains: "def _compute_stats"
    - path: "docs/data/stats.json"
      provides: "universe overview JSON consumed by stats.html (Wave 3)"
      contains: "score_distribution"
  key_links:
    - from: "process_ticker()"
      to: "overall_score(piotroski_f=, altman_z=, dcf_discount_pct=)"
      via: "kwarg pass of Wave-1 helper outputs after sector gating"
      pattern: "piotroski_f\\s*="
    - from: "write_json()"
      to: "docs/data/stats.json"
      via: "_compute_stats(df) then Path.write_text"
      pattern: "stats\\.json"
---

<objective>
Fold the Wave 1 distress + DCF helpers into the 4-pillar composite. Rewrite the Safety pillar to drop the `is_trap` veto and instead average Piotroski + Altman sub-scores (absent → 50.0 per D-04) with the existing defensive/debt-equity/current-ratio sub-scores. Add the sector applicability gate so financials/REITs get None (never zero) for excluded metrics. Wire forward + reverse DCF through process_ticker, score the DCF discount as a 4th Value sub-group, emit all new flat columns and nested score keys, and compute docs/data/stats.json for the Wave 3 stats page.

Purpose: This is the integration heart of Phase 7 — it turns the pure helpers from Wave 1 into ranked OverallScore inputs and produces the JSON the Wave 3 pages consume. Implements TRAP-03 (Safety driver swap), SECTOR-02/D-10/D-11 (sector matrix), the scoring half of SIGNAL-08/09 and DCF-01/02/03, and the data half of PAGE-02 (stats.json).

Output: Rewritten Safety pillar in overall_score() (is_trap removed, 17-leaf coverage), `_sector_allows()` gate, process_ticker DCF/distress integration + 6 new flat columns, extended write_json scores object, new `_compute_stats()` helper writing docs/data/stats.json, and updated/added tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-distress-signals-dcf-stats-snapshots/07-CONTEXT.md
@.planning/phases/07-distress-signals-dcf-stats-snapshots/07-RESEARCH.md
@.planning/phases/07-distress-signals-dcf-stats-snapshots/07-PATTERNS.md
@.planning/phases/07-distress-signals-dcf-stats-snapshots/07-01-SUMMARY.md
@.planning/phases/06-cheap-factors-sector/06-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (Wave 2):

**Modified `overall_score()` signature:** `is_trap` and `coverage_fraction`-as-Safety-driver removed from the Safety logic; `coverage_fraction` retained as a param (still informs Quality coverage); three new None-defaulted params added at the end: `piotroski_f=None`, `altman_z=None`, `dcf_discount_pct=None`. Inner scorers `_score_piotroski(f)` (None→50.0), `_score_altman(z)` (None→50.0), `_score_dcf_discount(d)` (None→None; <0→0.0; else ascending band). Safety = `_avg_present([piotroski_sub, altman_sub, def_sub_safety, de_sub_safety, cr_sub_safety])`. New return-dict keys: `piotroski`, `altman`, `dcf_discount` sub-scores; `value_dcf` sub-group.

**New helper `_sector_allows(fund, metric)` in `stock_screener.py`** — returns False when the row's sector excludes the metric ("dcf" or "altman").

**process_ticker() additions:** sector-gated calls to `_compute_piotroski`, `_compute_altman_z`, `_compute_dcf_forward`, `_compute_dcf_reverse`; new flat columns `Piotroski_F`, `Altman_Z`, `DCF_Intrinsic_Value`, `DCF_Discount_Pct`, `DCF_Implied_Growth`, `dcf_reverse_converged`, `score_piotroski_sub`, `score_altman_sub`, `score_dcf_discount_sub`. EV/EBIT + earnings_yield zeroed to None for Financial Services (D-11).

**write_json() additions:** nested `scores` keys `piotroski`, `altman`, `dcf_discount`; call to `_compute_stats(df)` writing `docs/data/stats.json` (compact, cache-bust-friendly).

**New helper `_compute_stats(df) -> dict`** + `STATS_PATH = Path("docs/data/stats.json")`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite the Safety pillar in overall_score() (drop is_trap, add Piotroski/Altman/DCF subs, 17-leaf coverage)</name>
  <files>stock_screener.py, tests/test_scoring.py, tests/test_distress_phase7.py</files>
  <read_first>
    - stock_screener.py lines 381-611 (current overall_score signature, Quality pillar scorers as the pattern to mirror, the existing Safety block 549-561 to replace, the all_sub_scores list 576-585, and the return dict 601-611)
    - stock_screener.py lines 297-331 (_piecewise_score, _winsorize, _avg_present)
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-01-SUMMARY.md (final SCORE_PIOTROSKI_BANDS, SCORE_ALTMAN_BANDS, SCORE_DCF_DISCOUNT_BANDS values from Wave 1)
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-PATTERNS.md ("overall_score() Safety pillar replacement" section — D-04 absent→50.0 inner-scorer pattern, all_sub_scores extension, signature extension)
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-RESEARCH.md (Safety Pillar Restructure lines 364-427; Coverage leaf count lines 383-396)
    - tests/test_scoring.py (existing 33 tests — find every test that passes is_trap= to overall_score; those become invalid and must be migrated)
  </read_first>
  <behavior>
    - overall_score() called WITHOUT is_trap (it is no longer a parameter); passing is_trap= raises TypeError
    - Safety pillar = _avg_present of 5 sub-scores: piotroski_sub, altman_sub (both always float — None→50.0 per D-04), plus defensive/debt-equity/current-ratio sub-scores (D-01b None→avg-over-present skip)
    - When piotroski_f and altman_z are both None, Safety still computes (those two leaves contribute 50.0 each) — Safety is never None purely from missing distress data
    - dcf_discount_pct scored as a 4th Value sub-group: None→None (absent), <0→0.0 (overpriced, D-01 negative-routing), else ascending band; Value = _avg_present([discount_group, yield_group, price_group, dcf_group])
    - coverage_pct denominator is 17 leaves (15 Phase-6 leaves − the old score_safety aggregate leaf + piotroski_sub + altman_sub + dcf_discount_sub)
    - return dict gains keys: piotroski, altman, dcf_discount, value_dcf
  </behavior>
  <action>
    Modify the overall_score() signature (lines 381-401): remove the `is_trap: bool` parameter entirely. Keep `coverage_fraction: float` (still referenced for Quality coverage and the meta inputs). Add three None-defaulted params at the end of the signature: `piotroski_f: int | None = None`, `altman_z: float | None = None`, `dcf_discount_pct: float | None = None`. Update the docstring SAFETY line to reflect the new architecture (Piotroski + Altman + defensive/de/cr).

    Add a 4th Value sub-group for DCF discount, mirroring the yield-group pattern (lines 464-475): inner `_score_dcf_discount(d)` → None if d is None; 0.0 if d < 0 (D-01 negative-routing — a negative discount means the stock trades above intrinsic); else `_piecewise_score(_winsorize(d, SCORE_DCF_DISCOUNT_WIN_LO, SCORE_DCF_DISCOUNT_WIN_HI), SCORE_DCF_DISCOUNT_BANDS)`. Compute `dcf_sub = _score_dcf_discount(dcf_discount_pct)`; `dcf_group = _avg_present([dcf_sub])`. Change score_value to `_avg_present([discount_group, yield_group, price_group, dcf_group])`.

    Replace the entire Safety block (lines 549-561). Add D-04 inner scorers that ALWAYS return float: `_score_piotroski(f)` → 50.0 if f is None else `_piecewise_score(float(f), SCORE_PIOTROSKI_BANDS)`; `_score_altman(z)` → 50.0 if z is None else `_piecewise_score(_winsorize(z, -999.0, 10.0), SCORE_ALTMAN_BANDS)`. Compute `piotroski_sub` and `altman_sub`. For the Safety pillar's defensive/de/cr inputs reuse the EXISTING def_sub/de_sub/cr_sub already computed in the Quality block (they feed both pillars by design — see the docstring "Intentional double-use" note; do not recompute). Set `score_safety = _avg_present([piotroski_sub, altman_sub, def_sub, de_sub, cr_sub])`. Remove references to SCORE_SAFETY_TRAP_PENALTY / SCORE_SAFETY_NOTRAP_BASE in this block (leave the constants defined but unused, annotated `# deprecated — Phase 7 Safety no longer uses the interim trap formula`).

    Update all_sub_scores (lines 576-583): remove the `score_safety` aggregate leaf; add `piotroski_sub`, `altman_sub`, and `dcf_sub`. Final list = lynch_sub, graham_sub, fcf_sub, earny_sub, shy_sub, s_52w_lo, s_52w_hi, s_5y_lo, dcf_sub, def_sub, de_sub, cr_sub, roic_sub, growth_g_sub, growth_stab_sub, piotroski_sub, altman_sub → 17 leaves. Update the inline comment to say "(17 leaves)".

    Extend the return dict (lines 601-611): add `"piotroski": round(piotroski_sub, 2)`, `"altman": round(altman_sub, 2)`, `"dcf_discount": round(dcf_sub, 2) if dcf_sub is not None else None`, `"value_dcf": round(dcf_group, 2) if dcf_group is not None else None`. Keep "safety" pointing at score_safety.

    Migrate tests/test_scoring.py: every existing call to overall_score(...) currently passes is_trap=. Remove the is_trap kwarg from all of them. For tests that specifically asserted trap-floors-Safety-to-0 behaviour, rewrite them to assert the NEW behaviour: a distressed Piotroski/Altman input drags Safety down proportionally (not a hard 0), and absent Piotroski+Altman → those leaves contribute 50.0. Add to tests/test_distress_phase7.py: overall_score with low Piotroski (e.g. f=1) + low Altman (z=0.5) yields a low score_safety; absent both → score_safety reflects 50.0 contributions from those two leaves; coverage_pct denominator is 17 (assert a fully-populated row reports coverage_pct == 100.0 and a Phase-5-only row reports the expected lower fraction).
  </action>
  <verify>
    <automated>cd "C:/Python Projects/graham_screener/.claude/worktrees/nifty-goldberg-0c9a42" && python tests/test_scoring.py && python tests/test_distress_phase7.py && python tests/test_scoring_phase6.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'is_trap' stock_screener.py` shows NO occurrence inside the overall_score() signature or Safety block (it may still appear in process_ticker as a diagnostic column — that is fine)
    - overall_score signature contains `piotroski_f`, `altman_z`, `dcf_discount_pct` as the last three params
    - The all_sub_scores list comment reads "17 leaves" and the list has 17 entries
    - `python tests/test_scoring.py` passes with NO call passing is_trap= (grep the test file: `grep -c 'is_trap' tests/test_scoring.py` returns 0 for calls — diagnostic-only references in comments allowed but no kwarg passes)
    - A distress test asserts low Piotroski + low Altman → score_safety well below 50; absent both → score_safety reflects 50.0 contributions
    - `python tests/test_distress_phase7.py && python tests/test_scoring_phase6.py` both exit 0
  </acceptance_criteria>
  <done>overall_score() no longer takes is_trap; Safety is the average of Piotroski/Altman (50.0 when absent) plus defensive/de/cr; DCF discount is a 4th Value sub-group; coverage is 17 leaves; all scoring tests pass under the new contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire DCF + distress into process_ticker() with sector gates, emit new columns, and compute stats.json in write_json()</name>
  <files>stock_screener.py, tests/test_distress_phase7.py</files>
  <read_first>
    - stock_screener.py lines 1473-1662 (process_ticker — the trap_gate call 1588-1593, the overall_score call 1599-1619, the flat-column block 1621-1654, the _r2 helper 1638-1639, fund["sector"] usage 1635)
    - stock_screener.py lines 1691-1723 (write_json — the nested scores object 1702-1714, the compact-write + min-row guard)
    - stock_screener.py lines 1053-1120 (get_combined_data — confirm how fund dict reaches process_ticker, where income_stmt_df / balance_sheet_df / cashflow_df from Wave 1 land in fund)
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-01-SUMMARY.md (exact helper signatures + discount-sign convention from Wave 1)
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-RESEARCH.md (Sector gate lines 534-564; stats.json schema lines 648-691; Pitfall 5 is_trap backward-compat lines 725-729; Pitfall 7 sector=None lines 737-741)
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-PATTERNS.md ("process_ticker() additions" + "write_json() additions" sections)
  </read_first>
  <behavior>
    - process_ticker calls _compute_piotroski with the two-year statement DataFrames threaded from get_yf_price_and_history (income_stmt_df/balance_sheet_df/cashflow_df — current year = columns[0], prior = columns[1])
    - For a Financial Services fixture sector: altman_z is None, DCF intrinsic/discount/implied are None, EV_EBIT and Earnings_Yield_Pct are None (D-11); Piotroski still computed
    - For a Real Estate fixture sector: DCF is None; Altman still computed; Piotroski still computed
    - For sector=None: no exclusion applied (all metrics attempted) — documented as intentional (Pitfall 7)
    - Row carries Piotroski_F (int|None, no rounding), Altman_Z, DCF_Intrinsic_Value, DCF_Discount_Pct, DCF_Implied_Growth (all _r2), dcf_reverse_converged (bool)
    - is_trap remains a flat column and scores.trap key (backward compat) but does not floor Safety
    - write_json writes docs/data/stats.json with score_distribution (5 buckets), pillar_averages, sector_breakdown, coverage_stats, buy_signal_count, low_safety_count, universe_count, generated_at
  </behavior>
  <action>
    In process_ticker (after the trap_gate call ~1593, before the overall_score call ~1599): read `sector = fund.get("sector") or ""`. Compute distress + DCF with sector gating:
      - `piotroski_f = _compute_piotroski(fund.get("income_stmt_df"), fund.get("income_stmt_df"), fund.get("balance_sheet_df"), fund.get("balance_sheet_df"), fund.get("cashflow_df"), fund.get("cashflow_df"))` — pass the same DataFrame for curr/prev; the helper reads columns[0] vs columns[1] internally per Wave 1 design (confirm against 07-01-SUMMARY; if the Wave-1 signature instead expects already-split single-year frames, adapt the call to match the SUMMARY).
      - `altman_z = _compute_altman_z(fund.get("balance_sheet_df"), fund.get("income_stmt_df")) if sector not in ALTMAN_EXCLUDED_SECTORS else None`
      - DCF: if `sector not in DCF_EXCLUDED_SECTORS`, call `_compute_dcf_forward(eps0, g, aaa_yield, price)` and `_compute_dcf_reverse(price, eps0, aaa_yield, g)` using the same eps/growth/price inputs already available in process_ticker (eps0 = current EPS used elsewhere; g = the realized 5yr CAGR growth value `g`; price = fund price); else set dcf_intrinsic=None, dcf_discount_pct=None, dcf_implied_growth=None, dcf_reverse_converged=False.
      - D-11 EV/EBIT + earnings_yield exclusion for Financial Services: if sector == "Financial Services", set the local earnings_yield and ev_ebit passed downstream to None (so the Earnings_Yield sub-score and EV_EBIT column become None for financials).

    Extend the overall_score() call (1599-1619): remove `is_trap = is_trap,`. Keep `coverage_fraction = cov_fraction,`. Add `piotroski_f = piotroski_f,`, `altman_z = altman_z,`, `dcf_discount_pct = dcf_discount_pct,`. (Note: is_trap is still computed above and still emitted as a flat column — only the overall_score pass is removed.)

    Add new flat columns after the existing Phase-6 block (~1654), using the existing _r2 helper: `row["Piotroski_F"] = piotroski_f` (int|None, no _r2), `row["Altman_Z"] = _r2(altman_z)`, `row["DCF_Intrinsic_Value"] = _r2(dcf_intrinsic)`, `row["DCF_Discount_Pct"] = _r2(dcf_discount_pct)`, `row["DCF_Implied_Growth"] = _r2(dcf_implied_growth)`, `row["dcf_reverse_converged"] = dcf_reverse_converged`. Also persist the new sub-scores for write_json: `row["score_piotroski_sub"] = scores["piotroski"]`, `row["score_altman_sub"] = scores["altman"]`, `row["score_dcf_discount_sub"] = scores["dcf_discount"]`. Keep `row["is_trap"] = is_trap` (backward compat).

    Extend write_json nested scores object (1702-1714): add `"piotroski": row.get("score_piotroski_sub")`, `"altman": row.get("score_altman_sub")`, `"dcf_discount": row.get("score_dcf_discount_sub")`. Keep `"trap": row.get("is_trap", False)` for backward compat.

    Add `STATS_PATH = Path("docs/data/stats.json")` near OUTPUT_PATH (~1688) and a `_compute_stats(df: pd.DataFrame) -> dict` pure helper computing the schema from RESEARCH.md lines 648-691: generated_at (same UTC format as results), universe_count, buy_signal_count (rows where Show is True), low_safety_count (rows where score_safety < 30 — `[ASSUMED]` threshold), score_distribution (5 buckets 0_20/20_40/40_60/60_80/80_100 over OverallScore), pillar_averages (mean of score_value/quality/growth/safety over non-null), sector_breakdown (array per sector sorted by count desc, null sector grouped as "Unknown", each {sector, count, avg_score, buy_signal_count}), coverage_stats (avg_coverage_pct + counts of non-null Piotroski_F/Altman_Z/DCF_Intrinsic_Value/FCF_Yield_Pct). In write_json, after writing results.json, call `_compute_stats(df)` and write it to STATS_PATH with `json.dumps(..., separators=(",", ":"))` (compact, matching results.json). Do not gate stats.json on the 100-row check separately — write_json already exits before this point if < 100 rows.

    Add tests to tests/test_distress_phase7.py: a process_ticker-level or _sector_allows-level test (use a small fund-dict fixture) asserting Financial Services → altman None + DCF None + earnings_yield None; Real Estate → DCF None, Altman present; sector=None → all attempted. A _compute_stats test: build a small DataFrame with known OverallScore/score_safety/Sector/Show columns and assert the bucket counts, low_safety_count, and sector_breakdown grouping are correct. (No network — fund-dict fixtures only.)
  </action>
  <verify>
    <automated>cd "C:/Python Projects/graham_screener/.claude/worktrees/nifty-goldberg-0c9a42" && python tests/test_distress_phase7.py && python tests/test_scoring.py && python tests/test_factors_phase6.py && python tests/test_scoring_phase6.py</automated>
  </verify>
  <acceptance_criteria>
    - process_ticker no longer passes `is_trap=` to overall_score (`grep -n 'is_trap' stock_screener.py` shows is_trap only in trap_gate, the row["is_trap"] column, and scores.trap — not in the overall_score call)
    - `grep -E 'Piotroski_F|Altman_Z|DCF_Intrinsic_Value|DCF_Discount_Pct|DCF_Implied_Growth|dcf_reverse_converged' stock_screener.py` matches all six column names
    - `def _compute_stats` and `STATS_PATH = Path("docs/data/stats.json")` both present
    - write_json scores object contains keys piotroski, altman, dcf_discount (grep the nested-object block)
    - A test asserts Financial Services → altman_z None AND dcf intrinsic None AND earnings_yield None
    - A test asserts Real Estate → dcf None but altman present
    - A _compute_stats test asserts score_distribution buckets sum to universe_count and low_safety_count counts rows with score_safety < 30
    - `python tests/test_distress_phase7.py && python tests/test_scoring.py && python tests/test_factors_phase6.py && python tests/test_scoring_phase6.py` all exit 0
  </acceptance_criteria>
  <done>process_ticker computes sector-gated Piotroski/Altman/DCF and emits the 6 new flat columns + 3 sub-score columns; write_json carries the new nested scores and writes docs/data/stats.json; sector exclusions and stats computation are tested.</done>
</task>

</tasks>

<verification>
- Full offline suite green: `python tests/test_scoring.py && python tests/test_growth_trap_fixes.py && python tests/test_factors_phase6.py && python tests/test_scoring_phase6.py && python tests/test_distress_phase7.py && python tests/test_dcf_phase7.py`
- `python -c "import stock_screener"` succeeds (with test API keys) — no signature/reference errors
- No test passes is_trap= to overall_score (the parameter is gone)
- The nested scores object and stats.json schema are additive — existing results.json keys are untouched
</verification>

<success_criteria>
- Safety pillar driven by Piotroski + Altman (absent → 50.0) + defensive/de/cr; is_trap no longer vetoes Safety
- Sector applicability matrix excludes DCF (financials + REITs), Altman + EV/EBIT + earnings_yield (financials) → None never zero
- Forward + reverse DCF wired through process_ticker; DCF discount scored as a 4th Value sub-group; 17-leaf coverage
- 6 new flat columns + 3 sub-score columns + 3 nested score keys emitted; is_trap retained as diagnostic
- docs/data/stats.json computed and written by Python (read-only consumption in Wave 3)
- All offline tests pass
</success_criteria>

<output>
Create `.planning/phases/07-distress-signals-dcf-stats-snapshots/07-02-SUMMARY.md` when done — record the final overall_score() signature, the final coverage leaf count, the exact stats.json schema/field names produced, the low_safety_count threshold used, and confirm is_trap is diagnostic-only.
</output>
