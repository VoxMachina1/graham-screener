---
phase: 07-distress-signals-dcf-stats-snapshots
plan: "03"
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - docs/app.js
  - docs/top.html
  - docs/stats.html
  - docs/history.html
  - docs/methodology.html
  - .gitignore
  - .github/workflows/screener.yml
  - stock_screener.py
autonomous: false
requirements: [PAGE-02, DATA-01, DATA-02, METH-01, TRAP-03]
must_haves:
  truths:
    - "The 5-link nav (Dashboard / Top Picks / Stats / History / Methodology) renders on every page"
    - "stats.html fetches docs/data/stats.json (cache-busted) and renders 5-bucket distribution + sector breakdown + coverage tables with no charting library"
    - "history.html fetches the snapshots manifest and lists dated snapshots with download links"
    - "top.html shows a Safety score chip and no longer shows the TRAP badge"
    - ".gitignore excepts docs/data/stats.json, docs/data/snapshots/*.json, and docs/data/snapshots/index.json"
    - "screener.yml commits a snapshot to docs/data/snapshots/YYYY-MM-DD.json only on the first weekday of the month, reusing the upstream min-row guard"
    - "methodology.html documents Piotroski, Altman Z'', DCF, and the sector applicability matrix while retaining all existing content"
  artifacts:
    - path: "docs/stats.html"
      provides: "universe overview page (PAGE-02)"
      min_lines: 40
    - path: "docs/history.html"
      provides: "snapshot list page (DATA-01 extension, D-13)"
      min_lines: 30
    - path: ".github/workflows/screener.yml"
      provides: "first-weekday monthly snapshot commit step"
      contains: "snapshots"
  key_links:
    - from: "docs/stats.html"
      to: "docs/data/stats.json"
      via: "cache-busted fetch + renderStats"
      pattern: "stats\\.json"
    - from: "docs/app.js NAV_ENTRIES"
      to: "all pages"
      via: "buildNav() array-driven 5-link nav"
      pattern: "key.*stats"
    - from: "docs/history.html"
      to: "docs/data/snapshots/index.json"
      via: "manifest fetch building the dated list"
      pattern: "snapshots/index.json"
---

<objective>
Ship the Phase 7 frontend and pipeline: extend the shared nav to 5 links, build the read-only stats.html and history.html pages, replace the top.html TRAP badge with a Safety score chip, add the .gitignore exceptions, wire the first-weekday-of-month snapshot step into screener.yml (with the snapshots manifest maintained in Python), and refresh methodology.html with the new signals. A human-verify checkpoint confirms the pages render correctly against the new stats.json/results.json before the phase closes.

Purpose: This is the user-facing payoff of Phase 7 — the distress/DCF data computed in Waves 1-2 becomes visible (Safety chip, stats page), the universe becomes observable over time (snapshots + history page), and the methodology is documented. Implements PAGE-02 (stats.html), DATA-01/DATA-02 (snapshots + manifest + min-row guard reuse + vintage caveat), METH-01 (methodology refresh), and the UI half of TRAP-03 (Safety chip replacing the trap badge per D-05).

Output: 5-link nav in app.js, new docs/stats.html + docs/history.html, top.html Safety chip, .gitignore exceptions, screener.yml snapshot step + Python manifest writer, updated methodology.html, and a verified rendering.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-distress-signals-dcf-stats-snapshots/07-CONTEXT.md
@.planning/phases/07-distress-signals-dcf-stats-snapshots/07-RESEARCH.md
@.planning/phases/07-distress-signals-dcf-stats-snapshots/07-PATTERNS.md
@.planning/phases/07-distress-signals-dcf-stats-snapshots/07-02-SUMMARY.md
@docs/top.html
@docs/app.js
</context>

<artifacts_this_phase_produces>
Symbols/files created by THIS plan (Wave 3):

**Modified `docs/app.js`:** NAV_ENTRIES extended to 5 entries (adds Stats + History before Methodology). `buildNav()` unchanged (array-driven).

**Modified `docs/top.html`:** TRAP-badge ternary (line 122) deleted; Safety pillar chip already present on row 2 (no new chip function needed per PATTERNS.md — fmtPillar(r.score_safety) covers it). Optionally colour the existing Safety chip by score band.

**New `docs/stats.html`:** Nord-themed static page; cache-busted fetch of data/stats.json; renderStats() drawing 5-bucket distribution cards, pillar-average cards, sector-breakdown table, coverage table. Calls buildNav("stats") + updateFreshnessUI(data.generated_at).

**New `docs/history.html`:** Nord-themed static page; fetch of data/snapshots/index.json; renders a dated list/table of snapshots with download links (data/snapshots/{filename}). Calls buildNav("history"). Documents the data-vintage caveat (DATA-02).

**Modified `.gitignore`:** three new exception lines (!docs/data/stats.json, !docs/data/snapshots/*.json, !docs/data/snapshots/index.json).

**Modified `.github/workflows/screener.yml`:** "Check if first weekday of month" step (id check-date) + "Commit monthly snapshot" conditional step (cp results.json to snapshots/YYYY-MM-DD.json, commit + push).

**Modified `stock_screener.py`:** snapshots/index.json manifest writer (Python maintains the manifest per D-13) — invoked from write_json or a dedicated helper; ensures docs/data/snapshots/ exists.

**Modified `docs/methodology.html`:** new sections for Piotroski F-Score, Altman Z'', two-stage DCF (forward + reverse), and the sector applicability matrix; all existing content retained.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend nav to 5 links, replace top.html TRAP badge with Safety chip, and add .gitignore exceptions</name>
  <files>docs/app.js, docs/top.html, .gitignore</files>
  <read_first>
    - docs/app.js lines 107-120 (NAV_ENTRIES array + buildNav)
    - docs/top.html lines 60-131 (fmtPillar at 60, the row-1 render with the trap-badge ternary at line 122, the row-2 pillar chips at 127-130 including the existing Safety chip)
    - .gitignore lines 1-7 (the *.json rule + existing !docs/data/results.json exception)
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-PATTERNS.md ("docs/app.js — NAV_ENTRIES" + "docs/top.html — Safety chip" + ".gitignore — new exceptions" sections)
  </read_first>
  <action>
    In docs/app.js, replace the NAV_ENTRIES array (lines 107-112) with the 5-entry version: Dashboard (index.html, dashboard), Top Picks (top.html, top), Stats (stats.html, stats), History (history.html, history), Methodology (methodology.html, methodology) — order exactly as listed (Stats + History inserted before Methodology). Do not touch buildNav() — it reads the array.

    In docs/top.html, delete the trap-badge ternary on line 122 (the r.is_trap conditional that renders the TRAP span). The Safety pillar chip already renders on row 2 (line 130 calls fmtPillar on r.score_safety) — keep it. Per D-05, the binary TRAP badge is replaced by the score chip. Optionally add a small colour treatment to the Safety chip based on score band (green >=60, yellow >=35, red below) using the colours from RESEARCH.md Pattern 4 — keep it minimal and consistent with the existing chip styling; if the existing pillar-chip styling already conveys enough, leave it uncoloured and record the choice in the SUMMARY. Remove the now-unused trap-badge CSS rule if one exists in top.html.

    In .gitignore, add three exception lines immediately after the existing results.json exception (line 5): an exception for docs/data/stats.json, one for docs/data/snapshots/*.json, and one for docs/data/snapshots/index.json (each prefixed with the gitignore negation bang).
  </action>
  <verify>
    <automated>cd "C:/Python Projects/graham_screener/.claude/worktrees/nifty-goldberg-0c9a42" && python -c "a=open('docs/app.js',encoding='utf-8').read(); g=open('.gitignore',encoding='utf-8').read(); t=open('docs/top.html',encoding='utf-8').read(); assert 'stats.html' in a and 'history.html' in a, 'nav missing'; assert 'trap-badge' not in t, 'trap badge still present'; assert '!docs/data/stats.json' in g and '!docs/data/snapshots/' in g, 'gitignore exceptions missing'; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - docs/app.js NAV_ENTRIES contains entries for stats.html and history.html (5 entries total, Stats + History before Methodology)
    - `grep -c 'trap-badge' docs/top.html` returns 0
    - .gitignore contains the three negation lines for stats.json, snapshots/*.json, and snapshots/index.json
    - The verify command prints PASS
  </acceptance_criteria>
  <done>Nav is 5 links across all pages; top.html shows the Safety score chip with no TRAP badge; .gitignore excepts the three new JSON paths.</done>
</task>

<task type="auto">
  <name>Task 2: Build docs/stats.html and docs/history.html, and add the Python snapshots/index.json manifest writer</name>
  <files>docs/stats.html, docs/history.html, stock_screener.py</files>
  <read_first>
    - docs/top.html (full — the page skeleton: head, Nord theme CSS link/inline, nav placeholder div, the fetch+render structure, the buildNav and updateFreshnessUI calls at the bottom of the script block)
    - docs/app.js lines 49-104 (escHtml, updateFreshnessUI, the freshness/format helpers shared by all pages)
    - stock_screener.py lines 1688-1723 (OUTPUT_PATH, write_json, the compact-write pattern; STATS_PATH + _compute_stats added in Wave 2 — confirm they exist)
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-02-SUMMARY.md (exact stats.json schema/field names produced in Wave 2)
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-RESEARCH.md (stats.json schema lines 648-691; index.json manifest schema lines 633-644; stats.html fetch pattern lines 796-810; D-14 stat-cards-no-charting; D-13 history page)
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-PATTERNS.md ("docs/stats.html and docs/history.html" section)
  </read_first>
  <action>
    Create docs/stats.html mirroring the docs/top.html skeleton exactly (same head, same Nord theme, same nav placeholder div, same script include of app.js). In the script: cache-busted fetch of data/stats.json (append a Date.now query param like results.json), then call updateFreshnessUI(data.generated_at) and a local renderStats(data). renderStats draws, using plain HTML built into container divs (NO charting library per D-14): number cards for universe_count / buy_signal_count / low_safety_count; a 5-bucket score-distribution row of cards (0-20, 20-40, 40-60, 60-80, 80-100) reading data.score_distribution; pillar-average cards reading data.pillar_averages; a sector-breakdown HTML table (sector, count, avg_score, buy_signal_count) reading data.sector_breakdown (escHtml the sector strings); a coverage table reading data.coverage_stats. Add a .catch that writes a "Could not load stats data." message into the container. Call buildNav("stats") at the end. Use the EXACT field names from the Wave 2 07-02-SUMMARY (do not invent field names — read the SUMMARY).

    Create docs/history.html mirroring the same skeleton. In the script: fetch data/snapshots/index.json (cache-busted), then render a dated list/table where each entry links to data/snapshots/{filename} (download link) with the date parsed from the filename. Handle the empty/missing manifest gracefully (show a "No snapshots yet." message — the manifest will not exist until the first first-weekday-of-month run). Add a visible data-vintage caveat sentence (DATA-02): note that snapshots reflect the data as of their date and methodology may have changed since. Call buildNav("history") at the end. Dead-simple per D-13 — no Tabulator, no charts.

    In stock_screener.py, add a snapshots manifest writer (Python maintains the manifest per D-13). Add `SNAPSHOTS_DIR = Path("docs/data/snapshots")` and `SNAPSHOTS_INDEX = SNAPSHOTS_DIR / "index.json"`. Add a helper `update_snapshot_manifest(filename: str) -> None` that ensures SNAPSHOTS_DIR exists, loads the existing index.json (or {"snapshots": []}), appends filename if not present, sorts the list, and writes it compact. This helper is invoked by the Actions snapshot step (Task 3) via a small CLI entry — add an `if __name__` reachable path or a module-level function the workflow can call with `python -c`. Do NOT call it during normal screener runs (snapshots only happen on the first weekday, driven by the workflow). Document this in the function docstring.
  </action>
  <verify>
    <automated>cd "C:/Python Projects/graham_screener/.claude/worktrees/nifty-goldberg-0c9a42" && python -c "s=open('docs/stats.html',encoding='utf-8').read(); h=open('docs/history.html',encoding='utf-8').read(); assert 'stats.json' in s and 'buildNav' in s, 'stats.html incomplete'; assert 'snapshots/index.json' in h and 'buildNav' in h, 'history.html incomplete'; import os; os.environ.setdefault('FRED_API_KEY','test'); os.environ.setdefault('FINNHUB_API_KEY','test'); import stock_screener; assert hasattr(stock_screener,'update_snapshot_manifest'), 'manifest writer missing'; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - docs/stats.html fetches data/stats.json, calls buildNav("stats") and updateFreshnessUI, and renders distribution/pillar/sector/coverage sections with no charting-library script tag
    - docs/history.html fetches data/snapshots/index.json, calls buildNav("history"), renders dated download links, includes a data-vintage caveat sentence, and handles a missing manifest
    - stock_screener.py defines update_snapshot_manifest and SNAPSHOTS_DIR/SNAPSHOTS_INDEX
    - `import stock_screener` succeeds with test API keys and exposes update_snapshot_manifest
    - The verify command prints PASS
  </acceptance_criteria>
  <done>stats.html renders the universe overview from stats.json; history.html lists snapshots from the manifest with a vintage caveat; Python can maintain the snapshots manifest.</done>
</task>

<task type="auto">
  <name>Task 3: Add the first-weekday monthly snapshot step to screener.yml and refresh methodology.html</name>
  <files>.github/workflows/screener.yml, docs/methodology.html</files>
  <read_first>
    - .github/workflows/screener.yml (full — the existing "Commit and push results" step at lines 34-42, the git config already set there, the cron schedule and permissions)
    - docs/methodology.html (full or via grep — find the section structure/heading pattern so new sections match existing styling and the existing content can be retained intact)
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-RESEARCH.md (snapshot workflow lines 568-631 incl. first-weekday shell logic + min-row-guard-is-upstream note; methodology.html section line 62/METH-01)
    - .planning/phases/07-distress-signals-dcf-stats-snapshots/07-PATTERNS.md (".github/workflows/screener.yml — snapshot step" section — git config already set, do not repeat)
  </read_first>
  <action>
    In .github/workflows/screener.yml, add two new steps AFTER the existing "Commit and push results" step (lines 34-42). Step A "Check if first weekday of month" (id: check-date): a run block computing DAY=$(date +%d) and DOW=$(date +%u), emitting is_first_weekday=true to $GITHUB_OUTPUT when DAY <= 7 AND DOW <= 5, else false (per RESEARCH.md lines 574-586). Step B "Commit monthly snapshot" guarded by `if: steps.check-date.outputs.is_first_weekday == 'true'`: compute SNAP_DATE=$(date +%Y-%m-%d); cp docs/data/results.json to docs/data/snapshots/${SNAP_DATE}.json; update the manifest by calling the Python helper from Task 2 (e.g. `python -c "import stock_screener; stock_screener.update_snapshot_manifest('${SNAP_DATE}.json')"`); git add the snapshot file + docs/data/snapshots/index.json + docs/data/stats.json; commit + push only if `! git diff --cached --quiet`. Do NOT repeat git config (already set by the earlier step). The min-row guard is upstream (write_json exits non-zero if < 100 rows, failing the job before this step runs) — add a comment noting this satisfies DATA-02; no separate guard needed here. Ensure docs/data/snapshots/ exists before cp (the Python helper mkdirs it, or add mkdir -p).

    In docs/methodology.html, add four new documentation sections matching the existing heading/section markup, while RETAINING all existing content (do not delete or rewrite existing sections): (1) Piotroski F-Score — the 9 criteria and how it maps to the Safety sub-score (absent → neutral 50.0); (2) Altman Z'' — the Z'' = 6.56·X1 + 3.26·X2 + 6.72·X3 + 1.05·X4 formula, the distress (<1.1) / safe (>2.6) zones, and that it is excluded for Financial Services; (3) Two-stage DCF — forward intrinsic value (5yr CAGR stage + Gordon terminal capped at 3%), WACC = AAA yield + 5.5% ERP, reverse DCF implied-growth gap, and the None-on-non-convergence behaviour; (4) Sector applicability matrix — which metrics are excluded per sector (financials: DCF/EV-EBIT/Altman; REITs: DCF) and that excluded metrics are treated as missing, never zero. Note the absolute-threshold scoring and that thresholds are [ASSUMED] config constants subject to tuning. Keep prose concise.
  </action>
  <verify>
    <automated>cd "C:/Python Projects/graham_screener/.claude/worktrees/nifty-goldberg-0c9a42" && python -c "import yaml; w=yaml.safe_load(open('.github/workflows/screener.yml',encoding='utf-8')); steps=w['jobs']['run-screener']['steps']; names=[s.get('name','') for s in steps]; assert any('first weekday' in n.lower() for n in names), 'check-date step missing'; assert any('snapshot' in n.lower() for n in names), 'snapshot step missing'; m=open('docs/methodology.html',encoding='utf-8').read(); assert 'Piotroski' in m and 'Altman' in m and 'DCF' in m, 'methodology sections missing'; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - screener.yml parses as valid YAML and contains a check-date step (id check-date emitting is_first_weekday) and a conditional "Commit monthly snapshot" step gated on is_first_weekday == 'true'
    - The snapshot step copies results.json to docs/data/snapshots/${SNAP_DATE}.json and calls update_snapshot_manifest; it does not repeat git config
    - A comment in the snapshot step notes the min-row guard is upstream (DATA-02)
    - methodology.html contains new sections mentioning Piotroski, Altman, DCF, and sector exclusions, with all prior content retained
    - The verify command prints PASS
  </acceptance_criteria>
  <done>The workflow commits a dated snapshot + updated manifest + stats.json only on the first weekday of the month; methodology.html documents the four new subsystems without losing existing content.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify the new pages render correctly</name>
  <what-built>
    Phase 7 frontend: a 5-link nav across all pages, a new Stats page (universe overview from stats.json), a new History page (snapshot list from the manifest), the Safety score chip replacing the TRAP badge on Top Picks, and a refreshed methodology page.
  </what-built>
  <how-to-verify>
    1. Serve docs/ locally: from the repo root run `python -m http.server 8000 --directory docs` and open http://localhost:8000/ in a browser. (results.json and stats.json must exist — if stats.json is not present yet because the screener has not run since Wave 2, either run the screener via Actions/workflow_dispatch first, or hand-place a small valid stats.json matching the Wave 2 schema for visual verification.)
    2. Confirm the nav on every page shows exactly five links in order: Dashboard, Top Picks, Stats, History, Methodology — and each link navigates correctly and highlights the active page.
    3. Open Stats (http://localhost:8000/stats.html): confirm the freshness badge shows, the 5 distribution buckets render as cards/numbers, pillar averages show, the sector breakdown table lists sectors with counts/avg score, and the coverage table renders. Confirm there is NO charting library loaded (view source — no Chart.js/D3/Plotly script tag).
    4. Open Top Picks (http://localhost:8000/top.html): confirm each card shows a Safety score chip and NO "TRAP" badge appears anywhere.
    5. Open History (http://localhost:8000/history.html): confirm it either lists snapshots with working download links or shows the "No snapshots yet." message gracefully, and that the data-vintage caveat sentence is visible.
    6. Open Methodology (http://localhost:8000/methodology.html): confirm the new Piotroski, Altman Z'', DCF, and sector-matrix sections are present AND all the pre-existing Lynch/Graham content is still there.
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass, or describe what rendered incorrectly.</resume-signal>
</task>

</tasks>

<verification>
- All static-asset checks in Tasks 1-3 pass (PASS printed by each verify command)
- screener.yml is valid YAML with the two new snapshot steps
- The human-verify checkpoint is approved
- No existing page (index.html, methodology.html prior content) is broken by the nav change
</verification>

<success_criteria>
- 5-link nav renders on every page via the shared NAV_ENTRIES array
- stats.html renders the universe overview from stats.json with no charting library (D-14)
- history.html lists snapshots from the manifest with download links + vintage caveat (D-13, DATA-02)
- top.html shows the Safety score chip; the TRAP badge is gone (D-05, TRAP-03 UI half)
- .gitignore excepts stats.json + snapshots paths; screener.yml commits monthly snapshots on the first weekday reusing the upstream min-row guard (DATA-01, DATA-02)
- methodology.html documents Piotroski, Altman, DCF, and the sector matrix with all existing content retained (METH-01)
- Human verification confirms correct rendering
</success_criteria>

<output>
Create `.planning/phases/07-distress-signals-dcf-stats-snapshots/07-03-SUMMARY.md` when done — record the final stats.json field names consumed by stats.html, the snapshot manifest format, the first-weekday detection logic used, and whether the Safety chip was colour-banded.
</output>
